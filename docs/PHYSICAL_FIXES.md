> **TestFlight September 13 — build 18:** signed archive/export/upload PASS at 21:27:41 Tbilisi. Version 0.1.0 (18) includes compact Calls with right-aligned timestamps, standard avatar fallbacks, default Contacts-name flow and redesigned voice/video call screens. All 489 automated tests, exact-source public CI and packaged/decoded-bundle secret checks pass. Apple processing is Complete, compliance and 1,326-character What to Test are saved. Mnelo Development shows **Testing** (1 existing tester); Mnelo Preview shows **Waiting for Review** (1 existing tester, automatic notification enabled). Superseded build 15 was removed from review and remains Ready to Submit, not expired. Build 17 was archived only and superseded by build 18. Physical installation and two-phone acceptance are still pending. [Exact evidence](BUILD_LOG.md) · [Physical checks](PHYSICAL_FIXES.md).

# Physical-test correction tracker — September 13

The owner reported the initial issues on build 9 and additional UI issues while only one phone had build 11. The corrections through voice/contact/menu and the new Me/Profile and shared-content library are included in signed TestFlight **0.1.0 (14)**, uploaded September 13 at 16:37 Tbilisi. Apple processing is Complete, Mnelo Development is Testing and Mnelo Preview is Waiting for Review. The internal TestFlight tester record shows Installed 0.1.0 (14); second-phone installation and functional acceptance remain pending. **Implemented does not mean physically verified.** The server transition is documented in [current rollout](DELIVERY_ROLLOUT.md).

| Reported issue                                 | Implemented change                                                                                                          | Actual verification / remaining check                                                                                       |
| ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| New-chat phone search and country code         | National-number lookup in the shared picker; inline country selector; existing permission-respecting address-book name wins | Automated phone/picker/identity tests; repeat on both phones                                                                |
| Unrecognizable key instead of saved name       | Verified phone mapping and locally saved contact names; encrypted profile/avatar synchronization                            | Signed HTTP/Signal application tests; physical names to confirm                                                             |
| QR scanner detects nothing                     | Explicit Expo Camera companion pods and defined ZXing provider; native picker/scanner flow retained                         | Final linked iPhone binary and purpose strings pass; scan both physical displays still required                             |
| Messages require both people to add each other | Addressed-envelope sender verification; first-contact projection; durable encrypted outbox/mailbox                          | Real Signal + signed HTTP tests include sender offline and no reciprocal manual add; real phones not migrated               |
| Call ends without connecting                   | Independent queued controls/SDP; separate TURN/WebRTC media; bounded ringing and persistent missed-call handling            | Native protocol probes and real browser voice/video through TURN pass; original two-iPhone failure needs fresh reproduction |
| Same-chat foreground alert and generic sender  | OS foreground alert suppressed; local in-app names; open conversation suppression                                           | Notification tests pass; background lock-screen alerts intentionally remain generic                                         |
| Bulky message times/status                     | Compact bubbles, swipe-revealed time, Mnelo leaf for delivered/read, no pending leaf                                        | UI/receipt tests; physical gesture/layout acceptance pending                                                                |
| Voice opens an extra form                      | Inline immediate recording, stop, preview, discard/send; failed draft retained                                              | Component/recording tests; microphone/preview/send on phones pending                                                        |
| Photos do not open/save                        | Full-screen image view, zoom, OS image share/save flow                                                                      | UI tests and native purpose descriptions; actual Photos saving pending                                                      |
| Attachment menu is oversized                   | Compact icon-and-label attachment actions                                                                                   | UI tests; physical appearance pending                                                                                       |
| Location only opens Apple Maps                 | Map-provider choice including Google Maps                                                                                   | URI/action tests; installed Google Maps/browser fallback pending                                                            |
| Reply/long-press unclear                       | Swipe reply, author/message quote, compact actions and reactions                                                            | UI/receipt/ordering tests; physical keyboard/reply acceptance pending                                                       |
| Intermittent delivery feels stuck              | Per-item persistent retry, round-robin pages, exact read IDs, resumable encrypted media and provider push jobs              | 131 device/protocol tests; cross-network/background testing on actual phones pending                                        |

Build 10 is signed for the two paired phones, with no uninstall or data reset. Source and server work is preserved in Git. The owner has authorized AGPL source distribution. Public binary distribution still requires corresponding-source/store compliance, complete key rotation/recovery and physical acceptance; the existence of a compiled build is not a public-release recommendation.

Build 11 is a standalone App Store-signed TestFlight candidate using the existing hosted preview services and encrypted delivery. Both participants must update in place before testing. Do not delete Mnelo, downgrade a migrated vault or use backup restore as a workaround. Follow the table above, including sender-offline delivery, cross-network calls, background/locked-screen notifications and photo/QR acceptance. Build and Apple availability evidence is in [BUILD_LOG.md](BUILD_LOG.md); a successful archive is not proof that an external tester has access.

## Voice, contact and menu follow-up — included in build 13

Voice recording now uses native microphone metering for a bounded, local waveform, compact timer and Mnelo-colored controls. Stopping retains the waveform and recording for playback, delete or send. Playback pauses during send/delete; a late audio response cannot restart playback after leaving the screen or disabling the preview. No transcription, AI processing or waveform upload is added.

Chat header name/avatar opens the Contact info route; the three-dot sheet is removed. The page shows the permission-respecting local contact name, known number/photo, message/voice/video actions, identity details and clear/block actions with separate destructive confirmation. Returning to the conversation does not overwrite the protocol name. Group titles open group details, where local-history clearing remains available.

Call actions, QR sharing and profile-photo actions use compact icon rows. Profile Save is in the header. QR explanatory copy no longer tells both people to scan each other. The shared action-sheet scrim remains stationary while only the opaque panel shifts 24 points; its native whole-screen slide is removed. Compose routes use normal page navigation instead of iOS modal card scaling. Reduced Motion and outside-tap dismissal are retained.

Verification: `npm run check` PASS — 320 Jest tests / 65 suites, 3 server tests, 132 device/protocol tests (455 total), TypeScript, lint, formatting, localization/security/environment and brand checks. iOS and Android Hermes exports PASS with dotenv disabled in the local diagnostic environment; these are not signed release archives. React Native Web component fixtures checked at 393×852 and 320×640 in EN/KA, including recording/preview, name navigation, call-sheet dismissal and no horizontal overflow. Screenshots are synthetic local fixtures, not evidence of native microphone or device acceptance.

Physical follow-up: microphone permission, silence/speech waveform response, stop/listen/delete/send, interrupted/background recording, Dynamic Type and Bold Text, contact-page back navigation, repeated sheet opening/closing, and QR/profile navigation on both phones. The immutable `ios-0.1.0-12` source/archive remains unchanged. These source changes are now in signed TestFlight build 13 and its immutable `ios-0.1.0-13` source tag; build 13 replaced build 12 in the beta-review queue. Internal availability is confirmed, while external approval and two-phone acceptance remain pending.

## Build 12 follow-up acceptance

Check external reaction badges, compact trailing delivery leaves, permission-respecting phonebook-name precedence, incoming/outgoing and red missed-call rows, native system typography, backdrop dismissal and photo safe-area close/save/share controls. Full source checks passed 447 tests; both Hermes exports, generic iOS Release compilation, signed archive/export, final permissions/signature/secret checks and Apple processing passed. Browser component previews used synthetic content at 320/390/430-point widths and EN/KA. Native Photos saving, touch targets, phonebook behavior and two-phone calling/delivery still require physical acceptance. The new peer-not-ready state preserves queued messages when a recipient has not initialized its new delivery keys. No server change was made for build 12.

## Own-profile reference follow-up — included in build 14

Me has a centered identity and grouped settings; tapping the name/photo opens a compact Profile overview. Name, About, Username and Links each open a focused editor; the number row opens existing phone verification, and Edit photo opens a dismissible icon menu. Existing profile values are preserved and unrelated fields are merged from the latest saved profile. All 461 automated tests and both diagnostic mobile exports pass. Actual screen components were checked with synthetic device/navigation adapters at 393×852 and 320×640 in EN/KA. Native photo/keyboard/back gestures and profile sharing still need physical acceptance. This work is now included in signed TestFlight build 14 and immutable source tag `ios-0.1.0-14`.

## Contact info and library follow-up — included in build 14

Contact info now uses the larger avatar/description hero and grouped actions. Media, links and docs opens a separate page with three tabs: a responsive photo grid (photos open/close/zoom/save/share), video file tiles, link rows and document rows. Entries come only from this conversation and page through older history; metadata queries avoid attachment bytes. Links are explicit HTTP(S), parsed locally without fetching previews. Document/video opening uses the system share sheet and cleans temporary files afterwards. Full suite: 466 PASS. EN/KA 393×852 and 320×640 component previews and tab/photo navigation pass; the final fresh browser run reports no errors.

Physical checks: contacts with/without an About and long names; scroll the library beyond 40 items; switch categories repeatedly and confirm another conversation never appears; open/close/save a photo; open a PDF/video using installed apps; test an empty library, offline access, larger text and native back gestures. Device screenshots and installed build-14 acceptance remain pending.

## Phonebook names follow-up — included in build 15

Limited Contacts access now keeps an action to choose the missing person; denied access opens Settings and full access offers a refresh. Copied direction marks no longer prevent complete international-number matching. Native contact changes and returning to Mnelo refresh names across the existing contact/chat/call projection. Number-search names update after selection and repeated lookup while preserving the separately saved Mnelo alias. All 472 tests and the signed archive/export pass; TestFlight 15 is Testing internally and Waiting for Review externally.

On each updated iPhone, check full and limited Contacts access, include the test person if needed, search their Georgian national/international number, rename them in Contacts, and return to the contact list, chat header, Contact info and Calls. Also test permission removal and restore. Verify that the user's local phonebook name does not change the other phone's display name. Neither physical device was installed or marked accepted by this upload.

## Default names and phone Contacts saving — build 16 uploaded; distribution verification pending

The owner confirmed the build-15 name changed correctly after allowing Contacts access. The follow-up now requests undetermined access on first contact use without a separate enable-names step. With access, verify the saved phone name appears automatically; without a match, verify the received Mnelo profile name/username or the number appears. Declining access must still allow messaging and must not reopen the permission prompt repeatedly.

Add a new test person through New contact, edit their local name and save. Confirm one matching number/name in iPhone Contacts and the corresponding Mnelo contact. Repeat Add and confirm no duplicate for an accessible entry. Try Contact info's Save to phone Contacts action, then rename/delete the entry in Contacts and return to Mnelo. Check denied and limited access, and preserve the existing phonebook name when the number is already saved. These new write flows have automated coverage and are included in the signed build-16 upload, but are not physically accepted. Native configuration was regenerated. Apple processing, compliance and tester availability still require verification after the owner restores the expired App Store Connect session.
