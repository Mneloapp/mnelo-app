# Historical Phase 0.1 evidence

These files were captured before the rename, under **Connecto, the former working name**. **Mnelo is the official product name.** The old project names, native identifiers, paths, package/Pod hashes and compiler diagnostics are retained verbatim as historical evidence, not active configuration.

See [current identity](../../PRODUCT_IDENTITY.md) and [rename verification](../../RENAME_REPORT.md). The baseline source hashes are not intended to match the renamed package metadata or regenerated native projects.
