> **Current TestFlight checkpoint — September 20, 2026, build 47:** uploaded at 00:33 Asia/Tbilisi; Apple processing **Complete** without Siri warning 90626. Both existing groups show **Testing** with 90 days remaining: Mnelo Development (1 internal tester) and Mnelo Preview (2 external testers). Standard-encryption/France-No answers are saved, Beta review was submitted, and no public App Store submission was made. This checkpoint supersedes the historical current-status paragraphs below.
>
> Compiled source `7717c6ec2415208c760910ad2fe96f59b34ac0c8`; [public tag ios-0.1.0-47](https://github.com/Mneloapp/mnelo-app/tree/ios-0.1.0-47) at `a13444a9570ed04e8d66a452f3f69b87b9edc5f0`; [exact-source CI 35467388881](https://github.com/Mneloapp/mnelo-app/actions/runs/35467388881) passed. All 875 host tests passed (659 Jest in 112 suites, 3 server, 213 device integration). Archive/export identity, signatures, distribution entitlements, endpoints, source offer, Testing-runtime absence, QR provider and both Siri vocabularies passed. Independent archive/IPA code, payload and all five app/extension dSYM comparisons passed. Source/decoded-Hermes scans had zero findings; two packaged minified-JavaScript matches were individually verified false positives, with raw evidence retained.
>
> IPA: 152,125,673 bytes, SHA-256 `adfa16e2bb065fc984bc614b80fcf51a975367d27bc80b924cfa9cae6301f286`; main Hermes SHA-256 `bf2e85c3779d749cff0073099bb3025f24292e3c66b27e5f0a6193ea076541c7`. Four vendor dSYM warnings remain for React, ReactNativeDependencies, WebRTC and hermesvm; app/extension dSYMs are retained. Build 46 is superseded; build 37 and later retained archives/IPAs are preserved. On September 20 the owner reported completing the requested build 47 call test and that it works well; phones were not connected, so no device logs or numerical latency were collected. Detailed upgrade/history preservation, notification behavior, other physical regression checks and native Message-button visibility remain unconfirmed. [Full evidence and physical acceptance](BUILD47_QA.md).

> **TestFlight September 13 — build 18:** signed archive/export/upload PASS at 21:27:41 Tbilisi. Version 0.1.0 (18) includes compact Calls with right-aligned timestamps, standard avatar fallbacks, default Contacts-name flow and redesigned voice/video call screens. All 489 automated tests, exact-source public CI and packaged/decoded-bundle secret checks pass. Apple processing is Complete, compliance and 1,326-character What to Test are saved. Mnelo Development shows **Testing** (1 existing tester); Mnelo Preview shows **Waiting for Review** (1 existing tester, automatic notification enabled). Superseded build 15 was removed from review and remains Ready to Submit, not expired. Build 17 was archived only and superseded by build 18. Physical installation and two-phone acceptance are still pending. [Exact evidence](BUILD_LOG.md) · [Physical checks](PHYSICAL_FIXES.md).

> **Current September 13 state:** the development ciphertext delivery service is deployed and enabled, while both physical phones remain on build 9. [Current rollout, data boundaries, evidence and open release gates](DELIVERY_ROLLOUT.md) supersede historical no-queue/undeployed statements below. Native iOS/Android Signal probes and separate TURN voice/video checks passed; physical migration, key lifecycle/recovery and binary distribution compliance remain unresolved. The owner has authorized open source; see [source licensing](OPEN_SOURCE.md).

> September 12 public-release request: [current readiness and exact blockers](PUBLIC_RELEASE_READINESS.md). Full automated suite 389 PASS, Doctor 21/21, no secrets. Apple confirms build 9 processing Complete. Public description/URLs saved as draft; no public submission. Second-device USB testing is the immediate acceptance step.

# Mnelo release configuration

> Build 8 (September 12): compact measured-label navigation and the owner's lower iPhone menu placement. Source `4caf344`; 389 tests PASS. Signed archive/export/upload PASS; Apple build `9e4f7ebe-a3ac-44bb-8d3b-7a52d58fcb08` processing **Complete**, compliance cleared, and **Mnelo Development internal group shows 0.1.0 (8) Testing**. What to Test saved. External build 5 remains Waiting for Review; physical installation and gesture/communication acceptance remain separate. See BUILD_LOG for artifact and signing evidence.

> Build 7 (September 12): latest typography/navigation package archived, exported and uploaded successfully. Apple build `a811a289-d128-4966-8b62-2c7a9e9bbde9` completed processing, compliance is cleared, and the existing Mnelo Development internal group has access. External selection is disabled while build 5 awaits Apple review; physical QA remains separate. See BUILD_LOG for exact evidence and latest delivery checkpoint.

> September 12 build 6: signed Release archive, App Store export and upload PASS for profile photos/cards, QR/associated domains and default notification enrollment. Apple build ID `85a1a06f-adb5-4a2b-b1c9-e0e8d68222db`. Apple processing is Complete, compliance cleared, and the existing Mnelo Development internal group has build 6. Apple blocks adding it to the external group until build 5 completes review. Actual artifact evidence is in [BUILD_LOG](BUILD_LOG.md). Physical universal-link/camera/card exchange and two-phone delivery/call acceptance remain required. [Update scope](PROFILE_CARDS_QR.md).

> September 12 owner-required correction: both platforms must support background/closed-app notifications and native incoming calls. See [BACKGROUND_DELIVERY](BACKGROUND_DELIVERY.md) for the new routing/privacy boundary, locked-device migration, lifecycle requirements and remaining delivery limits. Earlier no-push-registry/foreground-only notes are historical implementation checkpoints, not the accepted target. Build 2 is not ready under this acceptance requirement.

> Current September 12 update: the owner-authorized functional TestFlight beta has a scoped engineering review in [BETA_SECURITY_REVIEW](BETA_SECURITY_REVIEW.md). Exact development endpoints and the existing two-number admission are required; production and independent-audit claims remain blocked. Earlier blanket preview-blocking and absent-TURN statements below are historical. Hosted TURN now passes real browser UDP/TCP data/media tests; physical-device and Apple-distribution results are recorded separately.

**NOT READY for public release.** [Current TestFlight prerequisites and acceptance](TWO_IPHONE_TESTING.md) records the remaining two-iPhone gates. The owner has confirmed enrollment, app launch and notification permission on the first iPhone. Server registration confirms Production alert and VoIP routes; two-phone receipt, calling and device acceptance remain separate. The second tester is in Mnelo Preview awaiting build 5's external review; isolated reviewer sign-in accounts were prepared and submitted with that build. Later delivery checkpoints above supersede historical release blockers below. The device-owned migration supersedes former server-backed readiness reports.

Provider follow-up: Sandbox and Production APNs are configured on the existing development identity service with scoped keys and protected credential mounts; actual device receipt remains untested. Owner-confirmed Firebase setup created only Mnelo Development with Analytics disabled; Android app configuration and least-privilege FCM sender setup are incomplete. No Firebase conversation database, SMS Auth, paid product or new mobile binary was introduced by this configuration checkpoint. See [push operations](PUSH_OPERATIONS.md).

Identity remains Mnelo / mnelo / mnelo:// / com.mnelo.messenger / https://mnelo.com. EAS development profiles still create a development client. Production and unapproved preview endpoints remain blocked with `privacyRelease.reviewed = false`; the scoped functional beta accepts only the exact two-number development service configuration documented above. Non-local development requires a wss relay address.

September 11 owner-authorized signing update: Apple issued a development profile for `com.mnelo.messenger`; the local Xcode 26.6 build installed and rendered Welcome on the physical iPhone. This supersedes earlier local-signing blockers, but does not establish SMS, push, two-party communications or store readiness. No store listing, EAS project or distribution build was created. See [iPhone evidence and pending acceptance](IPHONE_DEVELOPMENT.md).

Active public environment variables are EXPO_PUBLIC_APP_ENV, EXPO_PUBLIC_RELAY_URL and EXPO_PUBLIC_PHONE_IDENTITY_URL. The phone identity endpoint permits HTTP only on loopback in local mode and requires HTTPS elsewhere. Vonage and Twilio credentials are server-only. Legacy Supabase variables are tolerated only in the local historical test environment and cannot activate the mobile backend. Hosted builds must not use or publish them.

Phone registration has a tested fictional-number fixture and a configured Infobip 2FA development adapter; Vonage/Twilio are retained alternatives. The owner authenticated, and a dedicated expiring 2FA key, single-use six-digit template and provider-side trial sending limits are configured. Real API readiness and signed local HTTP status passed; September 12 hosted HTTPS/WSS checks and the first physical iPhone SMS/code registration pass. The second phone, changed-number ownership, recovery and complete media acceptance remain pending. Before public release, validate delivery, review provider fraud/region/retention and scoped-runtime access, deploy a reviewed HTTPS identity service, harden limits across restarts and resolve recycled-number/lost-key enrollment. See [PHONE_IDENTITY](PHONE_IDENTITY.md). The owner funded Infobip with $20 and initiated one successful real registration. No branded sender was purchased. The earlier no-SMS/no-balance integration checkpoint is historical.

Before release:

1. Obtain independent review of the pinned-identity/SDP/WebRTC integration, group permissions, backup recovery and native lifecycle.
2. Deploy/audit wss signaling and encrypted TURN forwarding without identifying logs, durable queues, dumps, snapshots or retained content. Never claim that localhost tests prove hosting compliance.
3. Complete APNs/FCM provider configuration and physical background/cold-start acceptance of the implemented native call/alert paths. Minimal routing metadata is documented in BACKGROUND_DELIVERY; no conversation/history queue is permitted. Actual content still needs reachable participant devices. Do not infer device delivery from compilation or provider acceptance.
4. Test two physical phones across Wi-Fi/cellular, peer restart, group membership changes, permission denial, reconnect and restore. Test user-chosen iCloud/Drive save/restore separately. Automated cloud sync is not implemented.
5. Repeat secret scan, full current authorization suite, dependencies, final native builds and final cold/warm external-intent checks. Review the runtime decoder advisory.
6. Audit/decommission historical server infrastructure and any copies before a global no-storage promise. Preserve history in Git; do not expose old fixtures as production users.
7. Complete owner-controlled signing, store/legal declarations and publishing approval. No release or contractual action is implied by this engineering commit.

Builds and exact executed results belong in [QA_REPORT](QA_REPORT.md) and [BUILD_LOG](BUILD_LOG.md). A development build passing is not a public security guarantee.

Phone-first entry update (2026-09-11): fresh or previously unverified installations require SMS verification before any feature/peer runtime starts. Already enrolled installations reopen offline without SMS. Me exposes number change and local username/optional names; local usernames are not globally reserved/searchable. Archive restore does not carry enrollment. Before release, test real SMS receipt/autofill and changed-number ownership on physical devices with the intended HTTPS service origin. Development loopback/fictional-number validation is insufficient. Native identifiers, signing, provider credentials and store registration were unchanged by this update.

TestFlight preparation (2026-09-11): the owner authenticated in App Store Connect; Mnelo app record **6811153275** was created using the existing `com.mnelo.messenger` Bundle ID. Its draft status is Prepare for Submission, and TestFlight has zero builds. EAS submit configuration pins that exact app ID. No public release, upload, legal declaration, distribution certificate or new App ID was created. Hosting account access and deployment were subsequently completed; all service/review/device blockers are detailed in TWO_IPHONE_TESTING.md.

## Historical remote TestFlight preparation — 2026-09-12

This subsection records the earlier configuration-only checkpoint. The later signed archive, upload and first-phone standalone installation supersede its no-build/no-upload statements; see the current delivery result below.

The owner selected TestFlight for the remote second iPhone and future beta updates. `eas.json` now has a distinct **testflight** profile: store distribution, no development client, Release configuration, preview environment, incremented build numbers and the existing Apple target 6811153275. It uses only the isolated development HTTPS/WSS services for the two-tester cohort; production has separate environment configuration and does not inherit these URLs. The app maps this build profile to preview, which still requires the unchanged protocol-review gate. This is not a release relabeled as development.

Local preflight defaults to that profile. The installed EAS schema accepts it; the preflight currently exits 1 with **MESSENGER_SECURITY_REVIEW_REQUIRED**. It independently rejects a development-client profile for store upload. Endpoint syntax and a configured profile do not prove signing, a standalone build, Internet media or Apple approval. No new certificate, native artifact, cloud build, invitation or upload was made in this preparation.

After release prerequisites are satisfied, the intended EAS path is `eas build --platform ios --profile testflight`, followed by `eas submit --platform ios --profile testflight` for the exact completed artifact. EAS login/linking and Apple signing/owner declarations must be validated at that time; the local Xcode archive/upload path remains available. Never reuse the old Metro-dependent Debug app as a TestFlight artifact. The first external beta needs Apple's TestFlight review; testers do not need a registered device UDID or membership in the developer team. [Apple's workflow](https://developer.apple.com/testflight/).

For updates retain `com.mnelo.messenger`, its signing team/keychain access, the SecureStore name `mnelo.device.database-key.v1`, vault location and `history-v1.db` unless a tested migration deliberately changes them. Preserve enrollment service origin or implement its migration. Upgrade in place; do not delete the app or rotate its keys. Validate installation over the previous beta, retained registration/history, offline opening and old/new protocol compatibility before distributing each change. None of those update-preservation tests is claimed completed here. Exclude obsolete Supabase public dotenv values from the actual beta archive; current local development's unused publishable value is not an SMS/server secret but is unnecessary for the messenger.

## Current standalone delivery result — September 12

Version 0.1.0 (2) was built with Xcode 26.6, exported with Apple-managed App Store signing and uploaded to app 6811153275. Local Xcode signing/upload succeeded without an EAS account login or additional App ID. The existing first iPhone was updated in place and launched without Metro. Signature, matching keychain/app identity, installed build and process liveness were verified; screen/enrollment/media acceptance still requires physical QA.

The Apple upload delivery is d94ac49b-0aa4-4cdb-b806-40fcec0cfab3. Upload success does not establish TestFlight processing, export compliance, external review approval or an available tester download. None of those later results is claimed yet. See [current two-phone status](TWO_IPHONE_TESTING.md), [morning instructions](BETA_TEST_PLAN_KA.md), [owner encryption inventory](EXPORT_COMPLIANCE_TECHNICAL.md) and [artifact/build evidence](BUILD_LOG.md). No legal questionnaire was answered and no public release was submitted.

# Notification default update — September 12, 2026

The next build requests alert/sound/badge authorization automatically after verified registration and foreground entry. It preserves existing OS denial or revoked permission. Allow is still required by iOS; an upgrade cannot silently grant permission. Users manage the OS setting through Me > Notifications. Release QA must include a fresh installation (prompt after OTP only), Allow, Don't Allow, process restart without repeated denial prompts, re-enable/disable in system settings, and physical push delivery. No new native dependency or provisioning change is needed for this JavaScript update. Distributed builds are unchanged until a new binary is uploaded and installed.

## September 13 — Build 20 delivery checkpoint

Version 0.1.0 (20) is signed, exported and uploaded to the existing Mnelo TestFlight app. It includes the latest call, chat media, sharing, profile picker, QR, keypad and contact-search feedback. [Corresponding source ios-0.1.0-20](https://github.com/Mneloapp/mnelo-app/tree/ios-0.1.0-20) and [exact-source CI](https://github.com/Mneloapp/mnelo-app/actions/runs/34775148117) are verified. The new com.mnelo.messenger.share extension shares only its isolated incoming inbox with the main app; bundle/team, vault, history and hosted cohort remain unchanged. Build 19 is archived but unuploaded.

Apple processing is Complete. Mnelo Development shows build 20 Testing for its one existing tester. Mnelo Preview shows build 20 Waiting for Review for its one external tester, with automatic notification enabled. Build 18 was removed from the pending review without expiration and now shows Ready to Submit. This is an authorized existing-cohort TestFlight update, not a public App Store submission. New physical acceptance covers profile library after deletion, Photos/Files/Maps handoff, photo gallery gestures and two-phone calls; see TWO_IPHONE_TESTING and QA_REPORT. Expo's iOS receive-share bridge is experimental and needs testing on the supported physical OS versions.
