# Mnelo build log — current native migration

## September 12, 2026 — profile/QR native validation

Stack retained: Expo 57.0.22, React Native 0.86.3, React 19.2.3, Xcode 26.6, CocoaPods 1.16.2, Node 24.18.0/npm 11.16.0. Added compatible expo-camera 57.0.5 and react-native-svg 15.15.4, qrcode-generator 2.0.4 and test-only jsqr 1.4.0. Identity remains com.mnelo.messenger. Build number 6 is reserved; no upload or alteration of pending TestFlight build 5.

Native prebuild/pod install and the standard Xcode-signed arm64 Simulator Debug build completed. Final xcodebuild exited 0 with BUILD SUCCEEDED; the produced Info.plist reports CFBundleVersion 6. The permission checker also passed against the produced .app (all six keys and EN/KA translations), not only generated source configuration. Installed that final binary over the existing QA app; it booted Chats, retained the selected photo/profile and Georgian preference, and opened a fictional mnelo://contact invitation into the confirmation preview with Add contact disabled. This proves the custom-scheme path, not physical HTTPS universal links. Artifacts: `profile-final-prebuild.log`, `profile-final-pods.log`, `profile-ios-final-build.log`; app under `artifacts/profile-ios-derived/Build/Products/Debug-iphonesimulator/Mnelo.app`. This is a simulator development app, not a TestFlight IPA. Generated native projects remain ignored and reproducible from Expo config.

The first compile-only run with CODE_SIGNING_ALLOWED=NO built successfully but failed native vault/notification startup with ERR_NOTIFICATIONS_KEYCHAIN_ACCESS. A manual ad-hoc re-sign attempt then failed launch (NSPOSIX 163) and was discarded. The correct build uses Xcode's normal simulator signing with CODE_SIGN_IDENTITY=-; no permanent generated-code or Keychain bypass was retained. The correctly signed app booted SQLCipher/SecureStore and completed fixture registration.

Two earlier runs failed with ENOSPC. Only regenerable intermediates/index data were removed, including old build-derived intermediates; saved IPA/xcarchive files and application/user data were preserved. Third-party deprecation/script-phase warnings and Expo's source-build fallback remain in successful logs; success is not a zero-warning claim.

Native permission validation found that expo-camera's microphonePermission:false deletes NSMicrophoneUsageDescription shared by voice/calling modules, even when other plugins supply it. Set the camera plugin to the existing localized microphone description and regenerated iOS. The generated six purpose strings and EN/KA translations pass `check:ios:permissions`; no always/background location was added. The scanner itself requests camera only, not microphone.

Isolation finding: Expo SDK 57's `@expo/metro-config/build/transform-worker/transform-worker.js` virtual development env module imports root .env files with require.context independently of CLI EXPO_NO_DOTENV. Existing owner env files overrode the explicit fixture URL, so the first reserved-number attempt reached the development identity endpoint and was rejected instead of becoming a fixture. Owner env files and provider configuration were not edited. `metro.config.cjs` now excludes only root .env files from that context when EXPO_NO_DOTENV=1; ordinary developer launches retain normal dotenv behavior. Two regression tests pass. The fresh bundle contains the explicit local endpoint without file-env assignments, and the native OTP screen explicitly showed Development test only / No SMS sent before registration completed.

QA isolation: fresh simulator Mnelo Profile QA (iPhone 17 Pro, iOS 26.5), ephemeral loopback identity fixture on 8087 and own Metro on 8081. Public fictional test phone/code and the repository app icon were used; no personal photo or real SMS was used in the successful fixture flow. The owner's other simulator and 8083/8086 services were left intact.

Evidence: `profile-final-check.log`, `profile-final-doctor.log`, `profile-final-compatibility.log`, `profile-final-export.log`, `profile-final-secrets.log`, `profile-native-me.png`, `profile-native-qr.png`, `profile-native-qr-ka.png`. Process restart retained the profile and selected photo. Development Fast Refresh paused during native regeneration; restarting only the owned Metro and app restored the saved state. No vault bypass/data reset was used. New physical iPhone/Android camera and two-phone card exchange acceptance is pending; older successful message/call device evidence is not relabeled as this feature's QA.

2026-09-10: device-owned messenger migration. Xcode 26.6, SDK 57 stack preserved. Added expo-sqlite 57.0.2 with SQLCipher and local MneloVault native module. iOS prebuild without clean deletion and pod install succeeded (127 dependencies, 130 pods). `xcodebuild -workspace ios/Mnelo.xcworkspace -scheme Mnelo -configuration Debug -destination 'platform=iOS Simulator,id=3A981CD5-4BF5-4460-9F44-64A788301505' -derivedDataPath artifacts/ios-26.6-derived build` succeeded. Native boot, local database encryption evidence and real peer exchange are in [QA_REPORT](QA_REPORT.md).

Android prebuild and `npm run android:build` succeeded. Expo SQLite requested NDK 27.0.12077973, installed by Gradle using the existing licensed SDK setup. Build duration 7m40s, 576 tasks. Artifact: `artifacts/android/mnelo-development-arm64.apk`; local debug signing, ARM64 only. `npm run android:install -- emulator-5554` installed and booted it. Tunnels now cover only current Metro 8083 and signaling 8084, not old backend ports.

Warnings: Ruby 2.6 lacks Array.filter_map used by an Expo precompiled configuration reader, so source fallback occurred; native build still passed. Standard Expo/Hermes script-phase dependency warnings and Android SDK XML/Gradle deprecation warnings remain. None was hidden as a test success. Metro needs IPv4 localhost selection in this environment; use `NODE_OPTIONS=--dns-result-order=ipv4first`.

Raw logs: `artifacts/privacy-pods.log`, `privacy-ios-build.log`, `privacy-ios-final-build.log`, `privacy-android-build.log`, `privacy-exports.log`.

## Historical build findings — previous architecture

The entries below are historical native evidence; they do not prove the new privacy design or current peer behavior.

# Mnelo native build log

Current September 9 status: **Xcode 26.6 builds and boots Mnelo on iOS Simulator**. Current-lock Android Development Client and embedded diagnostic also build and render. Physical signing/device and cloud distribution remain open. Earlier failure entries below are dated evidence, superseded by the September 9 results and [native result](audits/ios-26.6/native-result.json).

## Native identity migration

Owner confirmation: no app-specific external registrations under the former working identity; migration authorized to `com.mnelo.app`. Both native projects are generated from Expo configuration. No external App ID or credential is created by prebuild.

## Existing compiler evidence

SDK 57 / expo 57.0.20, React Native 0.86.3, expo-modules-core 57.0.16, expo-modules-jsi 57.0.8, CocoaPods 1.16.2. Selected Xcode is 26.2 (17C52), at `/Applications/Xcode.app/Contents/Developer`, Swift 6.2.3. iOS deployment target is 16.4. SDK 57's published Xcode support floor is 26.4. Earlier clean prebuilds and pod installs still failed in ExpoModulesJSI RuntimeScheduler.h:61 with a Swift shared-reference annotation error and Xcode exit 65. Doctor and Metro export do not establish a native compile pass.

Versioned source: [Expo SDK 57 requirements](https://docs.expo.dev/versions/v57.0.0/#support-for-android-and-ios-versions). Exact historical source/compiler evidence: [Phase 0.1](PHASE_0_1_REPORT.md).

Current-host audit: Xcode remains 26.2, no authenticated Expo state or linked EAS project is available. The supported next local toolchain is stable Xcode 26.4+; no successful build with that toolchain is claimed. Product JS/TS development will continue under the owner's explicit instruction while native acceptance remains open.

## Fresh post-migration result

Clean prebuild generated both platforms with `com.mnelo.app`. CocoaPods installation passed. A fresh direct `expo run:ios --device … --no-bundler` invocation bypassed the local npm version guard and reached the real compiler. Result: **two compiler errors and one generated-script warning; Xcode exit 65**, in the unchanged ExpoModulesJSI Swift-reference annotation code. [Exact diagnostic](audits/native-migration/compiler.txt), [installed toolchain](audits/native-migration/toolchain.json), [generated identifiers](audits/native-migration/config.json).

This excludes stale product names/generated projects as the explanation. Dependency compatibility and Doctor passed; clean pods reproduced the failure. The selected Xcode/compiler is below Expo SDK 57's supported floor. No native patch, SDK switch or force-upgrade was applied. EAS is not authenticated/linked, so no cloud build was submitted. Update Xcode through Apple to stable 26.4+ and complete first launch, or authenticate EAS for a supported cloud development build when owner interaction is available. JS/TS feature work continues.

## Phase 4 — avatar native dependencies

Installed Expo-aligned ImagePicker 57.0.16, ImageManipulator 57.0.16 and FileSystem 57.0.6. Clean iOS/Android prebuild completed. Both generated package identifiers remain `com.mnelo.app`. iOS has the photo-library purpose string; camera/microphone remain disabled for this feature. CocoaPods completed with 114 Podfile dependencies / 116 total pods.

The host's Ruby 2.6 lacks Array#filter_map used by Expo precompiled module configuration. CocoaPods emitted external-config warnings and reported a source-compilation fallback (no precompiled xcframeworks found), plus upstream script-phase notices. This is not a native compile PASS. Use maintained Ruby/CocoaPods with the supported Xcode 26.4+ toolchain for the next native build. No generated source workaround was added. Both Metro mobile exports pass independently of native compilation.

## Phase 6 — native media configuration

Expo-aligned audio, document picker, location, contacts and sharing were installed; Doctor caught the missing direct expo-asset peer. Added expo-asset 57.0.16 and its explicit plugin to the dynamic app config. Doctor then passed 21/21. Clean prebuild for both platforms passed. This phase's Pods resolved 119 Podfile dependencies / 121 total pods; Ruby 2.6 Array#filter_map warnings still force source fallback. iOS privacy descriptions cover camera, microphone, photos, contacts and when-in-use location, with no always-location or background recording mode. Android blocks WRITE_CONTACTS and legacy external storage/overlay permissions. Metro exports passed on both platforms. These are preparation checks; the existing Xcode 26.2 compiler failure remains unresolved externally and no native capture/playback or device boot is claimed.

## Phase 14 — notification native configuration

Installed ExpoNotifications 57.0.17 and ExpoDevice 57.0.1 using SDK 57 alignment. Clean iOS/Android CNG prebuild PASS. CocoaPods PASS: 122 Podfile dependencies / 124 installed pods. Generated iOS entitlements include development aps-environment; app config selects production push environment for preview/production. Android library manifest contributes POST_NOTIFICATIONS; app config sets mnelo-updates as its FCM default channel. Bundle/package remain com.mnelo.app and scheme mnelo. The same legacy Ruby filter_map/precompiled fallback warnings remain; modules build from source. Doctor passes 21/21 with the documented Ruby/PATH environment, compatibility passes, both Metro native exports pass. Xcode 26.2 remains unsupported for this SDK; compilation/boot was not reclassified as passing. No cloud or device push success is claimed.

Phase 15: clean both-platform Expo prebuild PASS; CocoaPods 125 Podfile dependencies/128 pods PASS, including WebRTC-SDK 144.7559.10. Generated iOS background audio and camera/microphone descriptions checked. Legacy Ruby 2.6 filter_map warnings still prevent precompiled Expo modules, falling back to source. Doctor 21/21 and iOS/Android exports PASS with documented CocoaPods environment. Xcode 26.2 remains below SDK 57 requirement; no new native compile/boot/device PASS. See artifacts/phase-15-*-final.log (ignored) and [CALLS](CALLS.md).

Phase 16: no new native module/configuration. Doctor 21/21, compatibility and iOS/Android Metro exports passed. The supported-Xcode/native compilation/boot/device gates from Phase 15 remain unchanged.

Phase 17: no new native module. Doctor 21/21, dependency compatibility and both Metro mobile exports passed. Xcode 26.2 remains unsupported for this SDK 57 stack; these exports are not a native compile, simulator boot or physical-device pass.

Phase 18: no native module change. Doctor 21/21, compatibility and both mobile JavaScript exports passed; exported files were secret-scanned. Supported Xcode, native compilation/boot and physical-device checks remain pending.

Phase 19 uses existing native SecureStore and connectivity modules. Doctor 21/21 and compatibility passed. Both mobile JavaScript exports and secret scans passed; supported-Xcode/native boot/physical checks remain pending.

Phase 20: Doctor initially failed when CocoaPods was absent from PATH. With the documented Ruby/CocoaPods environment it passed 21/21; dependency compatibility and both JS exports passed. No native compiler change or native startup/FPS/boot PASS is claimed. Native build gate remains Xcode 26.4+.

Phase 21: both platform JavaScript exports, Doctor 21/21, compatibility and secret scans pass. Browser QA found stale hot-update styling alongside an Expo CLI/client synchronization warning; a fresh reload confirmed all three final tab targets at 48 px. This is not native touch/Dynamic Type or compiler validation.

Phase 22: clean both-platform prebuild and CocoaPods (125 dependencies/128 pods) passed. Verified generated en/ka InfoPlist.strings, CFBundleLocalizations and Android locales_config/resources. Existing Ruby source-fallback warnings persist. Doctor 21/21, compatibility and mobile JS exports/scans pass; native compile/boot remains blocked by unsupported Xcode 26.2.

Phase 23 current preflight: Xcode remains 26.2/17C52, selected developer path unchanged, Swift 6.2.3. npm preios correctly exits 1; EAS CLI 23.2.0 reports Not logged in; paired iPhone is now unavailable. No repeated export or browser test is presented as native device acceptance. See IPHONE_DEVELOPMENT.md.

## Phase 24 — first successful native Android build and boot

Installed isolated checksum-verified Temurin 17.0.20.1+1, Google command tools 15859902/22.0, platform/build tools API 36/36.0.0, NDK 27.1.12297006 and CMake 3.22.1. Generated Gradle 9.3.1 reports Kotlin 2.1.20/KSP 2.1.20-2.0.1, compile/target SDK 36 and min SDK 24. No global Java change, Google Play registration or production signing occurred.

The first actual ARM64 :app:assembleDebug failed after 12m15s at :app:processDebugResources: `resource drawable/splashscreen_logo (aka com.mnelo.app:drawable/splashscreen_logo) not found`. The Expo splash plugin had only a background color. Added an explicitly temporary system-letter M PNG to the authoritative app config as icon/splash image, then regenerated. This is a source configuration fix, not a generated-code patch. Initial placeholder rendering was caught as black during visual inspection and corrected to an opaque warm-white/dark-green M; it is not final brand artwork.

Build after the configuration fix: BUILD SUCCESSFUL in 2m55s, 533 actionable tasks. Final corrected-asset incremental build: BUILD SUCCESSFUL in 16s, 533 tasks (26 executed, 507 up-to-date). Artifact: `artifacts/android/mnelo-development-arm64.apk`, 105,374,301 bytes, SHA-256 `4f2e0a23edfab55432061140aba9c7f250ad4733e1fba923485b713ebb0bc3f4`. Debug-signed Development Client, ARM64 only, com.mnelo.app, label Mnelo, version 0.1.0/code 1, min 24/target 36. apksigner verification and zipalign -P 16 pass; all 25 native ELF files have LOAD alignment at least 16 KiB. This is not a Play AAB or a test on a 16-KiB-page device. APK preserved before removing this project's generated caches for emulator disk space.

Actual AOSP API 36 ARM64 emulator 37.1.11 booted, APK installation succeeded, adb reverse connected only local Metro/Supabase/LiveKit ports, and the Mnelo welcome screen rendered. Expo Development Client is the native host. No Expo Go was used. Real local OTP authentication, Chats, two-way messaging/read state, camera denial then synthetic camera capture/JPEG upload, document picker/text-file upload, microphone denial/recovery, Devices and English/Georgian screens were exercised. A pending text survived API-tunnel removal and force-stop; after restoring connectivity and launching again, native SecureStore restored Auth and the pending text committed exactly once. An independent database query found exactly one row for each of four phase text fixtures. The peer reply sent during interruption was retrieved and read. Emulator airplane mode leaves a virtual Ethernet path; removing the API tunnel is the verified network failure, not physical airplane-mode certification.

Native visual QA found and fixed bottom navigation overlap, the Android edge-to-edge keyboard hiding the composer, absent Hermes Intl.DisplayNames country-name support, emoji-style phone glyph and large-font Georgian tab truncation. Shared SafeAreaView now protects stack-screen bottoms, KeyboardAvoidingView uses Android height behavior, country labels use pinned licensed English/Georgian CLDR data, phone glyph requests text presentation and large tab labels wrap to two lines. The 840×1680 / 200% system-font check remained scrollable to Connect controls; default 1080×2400/font 1 restored afterwards. Empty media captions also exposed a browser View-text warning; Boolean conditional rendering fixes it and the reopened conversation console is empty.

Upstream SDK XML-version, Gradle deprecation and Kotlin/Java deprecation warnings are retained in ignored phase-24 build logs. No errors were suppressed. Native JavaScript log review and remaining calls/location/push/background checks continue in final QA; this phase does not certify all device flows. Headless emulator audio was disabled, camera was synthetic, no FCM/APNs delivery was performed and no physical Android was connected. iOS remains blocked by Xcode 26.2/EAS authentication as documented above.

## Phase 27 — native E2E follow-up

The Phase 24 development APK was installed on a fresh Google Play API 36 ARM64 revision 7 image with Emulator 37.1.11. SDK package: `system-images;android-36;google_apis_playstore;arm64-v8a`; official archive `arm64-v8a-36_r07.zip`, 1,886,527,965 bytes, repository SHA-1 `5b91ef80a5eb60d7b7167528db259c2a12d20e9c`. SDKmanager installed package.xml successfully after accepting the local SDK image license; its first invocation had skipped the new license despite exit 0. This was not Google account authentication or a Play application registration.

The original AOSP image lacked Play services and could not satisfy Expo Location's installed fused provider (SERVICE_INVALID). The replacement AVD `mnelo_api36_google` uses Pixel 7 dimensions and synthetic cameras. Emulator enforces a 6-GiB userdata partition even when a smaller value is requested; initial boot failed at 7,279 MB available versus 7,372.8 MB required. Only the obsolete task-created AOSP AVD/image were removed, retaining the local database and QA artifacts. The next boot completed, APK installation succeeded and Mnelo rendered. The emulator increases its RAM to 2,560 MB. No physical Android result or 16-KiB page-size runtime result is implied.

Fresh native OTP login succeeded after an actual rate-limit response and a subsequent allowed retry; no rate limit was disabled. Denied WebRTC microphone permission now displays the correct recovery guidance and does not initiate a call. The fix handles the installed SDK's non-Error MediaStreamError and incomplete track results; no generated native source was patched. See END_TO_END_QA for final flow details and remaining hardware/cloud gates.

## Phase 30 — final local Android artifacts

A local standalone diagnostic path (`npm run android:diagnostic`) runs `:app:assembleRelease` with ARM64, embedded JavaScript and debug signing. It refuses non-local environments. This is not the EAS preview profile or a store artifact. The authoritative config plugin permits cleartext only for localhost/127.0.0.1 in the local Android network-security resource; hosted builds remove that resource reference and set cleartext false. Actual Expo introspection verified both configurations. Optimized local builds now retain the test-account notice and reserved local phone flow even when **DEV** is false; hosted validation and mock-repository production denial remain enforced.

The first diagnostic and development attempts stopped at the five-GiB disk preflight, before compilation. Successful diagnostic build: 6m 46s, 904 tasks (857 executed, 47 up-to-date). Successful final development build: 3m 44s, 533 tasks (62 executed, 471 up-to-date). Upstream Kotlin/Java/C++ and Gradle deprecation warnings remain in the retained logs; no warning suppression or compiler patch was added.

Diagnostic artifact: `artifacts/android/mnelo-local-diagnostic-arm64.apk`, 60,158,889 bytes, SHA-256 `a293d139f84c398797e197b9b3d233b783e94a1354216f5e76e7a66c20bfe5eb`. Embedded `assets/index.android.bundle` present; com.mnelo.app, Mnelo, version 0.1.0/code 1, target/compile API 36, ARM64. Signature verification and 16-KiB ZIP alignment pass; all 24 native ELF libraries have LOAD alignment at least 16 KiB. No 16-KiB runtime claim.

Development artifact: `artifacts/android/mnelo-development-arm64.apk`, 105,371,750 bytes, SHA-256 `8572259bba15ee4b2fd7e8346f79fe558018eaffc43e776ecac2d727ccec4c0c`. This replaces the Phase 24 APK at that path; its older hash remains historical evidence. Build logs and final machine-readable hashes are under ignored `artifacts/phase-30-*`.

To preserve host space after verifying the APK copies/hashes, only generated resources were removed: this repository's ios/Pods (Podfile.lock and copied Manifest.lock retained), task Gradle caches, android/app/build, generated Android build/.cxx directories for five installed native modules, and the disposable test AVD. An unused task postgres-meta image was removed; live backend containers/data were untouched. Guest free blocks were trimmed. Native dependency source, lockfiles, APKs, screenshots and logs remain. Future native builds regenerate these outputs; run pod install with supported Xcode/Ruby before the next iOS build. This cleanup is not a successful iOS compilation claim.

A fresh `mnelo_api36_google` AVD is used for final boot verification. AVDmanager's default ten-GiB userdata setting initially required 12,288 MB free and failed; explicitly restoring a six-GiB partition, two cores and 2,048-MB requested RAM avoids that unnecessary allocation. Emulator raises RAM to 2,560 MB. Final boot/launch observations follow below.

Final standalone boot PASS: the fresh Google AVD installed the diagnostic APK, started MainActivity directly and rendered the Mnelo Welcome screen with editable positioning and the explicit local test-account notice. Only API/LiveKit ports were reversed; no Metro tunnel existed. A 6,153-byte malformed cold native URI returned Status: ok and rendered the app; Continue opened Phone. The same warm URI preserved the process and safely returned to Welcome; subsequent interaction worked. Real local OTP then reached Chats. Force-stop/restart restored the authenticated session. The captured standalone process log contained zero fatal/React Native error lines. Screenshots and intent/runtime evidence are under phase-30-standalone-* and phase-30-_-intent._.

Final Development Client boot PASS: installed the newly built development APK over the same local package, connected the explicit Metro tunnel, dismissed the Expo development-menu introduction, and rendered Chats followed by Me with the restored Development Nika profile. Fresh native UI dumps and viewed screenshots confirm exactly Chats/Connect/Me and the local test notice. Its 328-line process log contains zero fatal/React Native error lines; this bounded log capture is not an assertion that no warning ever occurs.

Both APK signatures and ZIP alignment pass. The compiled diagnostic XML resource was found through the optimized resource map (`res/z3.xml`); its base denies cleartext and its only domain exceptions are localhost and 127.0.0.1, without subdomains. An initial lookup using the unobfuscated source filename found no file because release resources are renamed; it was an inspection-path issue, not a build failure. Redacted Gitleaks scanning of the actual embedded diagnostic bundle and its extracted strings found zero findings. The final installed package/native inventory matches the lockfile and is committed under docs/audits/phase-30/.

Final local iOS preflight still fails on Xcode 26.2; no additional compiler success is claimed. EAS remains unauthenticated. Supabase CLI lists pre-existing projects but organization-plan access returns 403 and dashboard sign-in is required; no isolated mobile cloud project or billing change was made. See RELEASE_REPORT for the exact next owner action.

## September 9 — resumed local iOS validation after the owner's Xcode upgrade

The preceding September 7–8 toolchain observations are historical. Selected Xcode now reports **26.6 (17F113)** at `/Applications/Xcode.app/Contents/Developer`, Swift **6.3.3**. First-launch status and the SDK-specific npm preflight pass. Xcode lists iOS/Simulator SDK 26.5. An existing iOS 26.2 iPhone 17 Pro Simulator boots, but the first concrete-destination and generic-simulator builds both stop **before compilation, exit 70**, because Xcode reports `iOS 26.5 is not installed`. This is a newly observed missing-component prerequisite, not the former ExpoModulesJSI compiler error. The official pinned `-downloadPlatform iOS -buildVersion 26.5` could not resolve that download; unpinned `xcodebuild -downloadPlatform iOS` resolves Apple's iOS 26.5 Simulator (23F77), arm64, 8.52 GB. Installation and full app compilation are still in progress at this checkpoint.

Restored Pods with the documented scoped Ruby environment. Before maintenance alignment, `pod install --deployment` passed and preserved the prior lock exactly. Doctor then detected the September 8 recommended Expo patches (initially 20/21). Official dependency alignment selected expo 57.0.21, @expo/ui 57.0.17 and expo-router 57.0.20, with required expo-modules-core 57.0.17 and expo-modules-jsi 57.1.0. SDK stays 57; React Native stays 0.86.3. Incremental CNG prebuild and pod install passed, 125 dependencies/128 pods; the installed Manifest.lock matches Podfile.lock. Ruby 2.6 `filter_map`/precompiled warnings still select source compilation. No generated code, compiler diagnostics or security checks were disabled.

Maintenance commit **14312ca** passed strict TypeScript, lint, formatting, source/environment/localization guards, 164 Jest tests/32 suites, three server-stream tests, Doctor 21/21, Expo compatibility, both mobile exports and zero-finding history/source/bundle secret scans. npm audit retains 16 moderate and zero high/critical findings, from the same two advisories. [Maintenance evidence](audits/ios-26.6/maintenance.json).

To separate the compiler change from dependency updates, the original expo-modules-jsi 57.0.8 tarball was verified against its historical lockfile integrity. Its RuntimeScheduler.h is byte-identical to 57.1.0 (SHA-256 `ab3e6488b7e527210ecc1c8c58318c1e0ac5db3a489ddcc8ed5a11eaf6f7e5fb`). Swift 6.3.3 successfully typechecked the original header import plus a RuntimeScheduler constructor/method call with C++20 interoperability, targeting arm64 iOS 16.4 Simulator. The formerly fatal annotations now produce two warnings at lines 53/61; no diagnostic was suppressed. This narrow result is **not** a full framework or app build. The original package's full Xcode framework command also stopped at the missing-platform exit 70. [Isolated probe evidence](audits/ios-26.6/header-probe.json).

The local development-server preparation exposed a separate Node/Expo localhost mismatch: Node bound only `::1`, while the manifest advertised `127.0.0.1`. Both the refused IPv4 request and working IPv6 request were observed. Restarting only this task's Metro with scoped `NODE_OPTIONS=--dns-result-order=ipv4first` and `--localhost --port 8083` restored both loopback hostnames. Its advertised iOS development bundle returns HTTP 200 (12,212,777 bytes); the local Supabase Auth health endpoint also returns 200. No LAN exposure, backend reset or production-environment exception was introduced.

Xcode's existing Apple account shows one Admin Developer Team with Certificates/Identifiers/Profiles access; provisioned-device lookup fails, including one retry. The paired physical iPhone reports iOS 26.6.1, Developer Mode enabled and disconnected. No signing credential was exported, app-specific provisioning verified, old App ID reused, or other published app changed. Expo/EAS account authentication is **optional for the current local compilation route**; Expo Push project/credential setup remains a later separate prerequisite. See [current iPhone workflow](IPHONE_DEVELOPMENT.md).

The official download subsequently completed successfully. The original 57.0.8 ExpoModulesJSI package's unmodified build script then built its complete simulator xcframework in **18 seconds, exit 0**, under Xcode 26.6 with the unchanged React Native 0.86.3 headers. Annotation, umbrella-header and deprecated dynamic-lookup warnings remain visible in the log. This establishes a full original-framework compiler comparison, in addition to the isolated header probe.

The new runtime initially disappeared after being listed: boot failed with `Unable to boot device because we cannot determine the runtime bundle` (exit 2), and a concrete app build exited 70 for the missing destination. A generic app build reached native resource compilation but exited 65 at expo-dev-menu's asset catalog: `No simulator runtime version from ["22D8075", "23C54"] available to use with iphonesimulator SDK version 23F81a`. CoreSimulator reported two registrations for the exact same newly downloaded 23F77 disk image: one explicitly `Unusable` / `Duplicate`, the other `Ready`, although the runtime bundle path did not exist. A dry run confirmed the single unusable registration; `simctl runtime delete <duplicate> --keep-asset` followed by `simctl runtime scan-and-mount` removed only that duplicate and preserved the downloaded asset. The canonical runtime then reported Ready, the bundle existed and iOS 26.5 appeared as available. No device data or unrelated runtime was deleted. A new full app build and 26.5 boot follow this repair; the older 26.2 simulator independently booted successfully too.

### Successful iOS Development Client compile and native boot

After the runtime repair, the full arm64 simulator build under Xcode 26.6 succeeded in **837.79 seconds**, exit 0, with **955 warnings / zero errors**. The diagnostic command used `CODE_SIGNING_ALLOWED=NO`; it installed but could not initialize native secure storage. Actual runtime failures were Expo Notifications `ERR_NOTIFICATIONS_KEYCHAIN_ACCESS` and ExpoSecureStore `KeyChainException`, Keychain code **-34018**, followed by the app's “Could not load this” boundary. Compilation alone did not count as a boot pass.

Rebuilt the unchanged application with normal simulator ad-hoc signing (`CODE_SIGNING_ALLOWED=YES CODE_SIGN_IDENTITY=-`). This incremental build succeeded in **44.56 seconds**, exit 0, **519 warnings / zero errors**. Strict deep signature verification passes. Xcode generates the simulator application-identifier entitlement ending in `com.mnelo.app`; the physical-device entitlement/provisioning path is separate. No manual Keychain entitlement patch, SecureStore fallback, compiler suppression or account registration was used. Upstream warnings include Swift/nullability, concurrency, deprecations, pod C/C++ diagnostics and script-output notices; full logs and xcresults are retained under ignored artifacts.

Installed and launched the signed Development Client using Expo CLI's existing-binary path with the documented Ruby environment and IPv4 loopback Metro. `--no-bundler` cannot be combined with `--port` in this installed CLI; the valid existing-binary invocation uses `--no-install --port 8083`. Expo/EAS authentication and Expo Go were not used. The actual native welcome screen rendered Mnelo and editable positioning with the local test-account notice. Native OTP rejected a wrong code, then authenticated Development Nika against local Supabase. Process termination/relaunch restored the session through SecureStore. Chats/Connect/Me, profile data, English and Georgian software keyboards, outside-tap keyboard dismissal, deterministic request interpretation, three actual development-database candidates and a data-backed match profile were viewed.

A temporary keyboard-layout hypothesis was measured against the original component: both produced the same 408-point scroll viewport and 618-point content. The experimental component changes and diagnostic logging were reverted completely; no layout fix is claimed. Native input and dismissal were exercised, but this is not full keyboard/gesture, Dynamic Type, VoiceOver or physical-device QA.

Evidence: `artifacts/ios-26.6-simulator-verified-build.log`, `artifacts/ios-26.6-simulator-signed-build.log`, corresponding `.xcresult` bundles, `artifacts/ios-26.6-welcome.png`, `artifacts/ios-26.6-connect-results.png`, and [redacted native results](audits/ios-26.6/native-result.json). The build input is maintenance commit `14312ca`; later changes in this checkpoint are documentation/evidence only. Physical iPhone remains disconnected; no IPA, TestFlight or real push/media/call hardware pass is claimed.

### Standalone iOS native-input validation

A separate **Release-configuration local simulator diagnostic** with embedded JavaScript and normal ad-hoc signing built successfully: **450.93 seconds wall time / 447.33 seconds in xcresult, zero errors / 1,032 warnings**. It is not a hosted preview, IPA or store build. The embedded Hermes bundle is 7,210,007 bytes, SHA-256 `9ee66ecb44ff1f5dfeb5425ceffe55a9691a7bc1a78fcd26b40de1998bb82eb9`. Signature verification passes; display name Mnelo, bundle com.mnelo.app, version 0.1.0/build 1, minimum iOS 16.4, arm64 Simulator.

Two task-owned Metro listeners were found after the earlier `expo run:ios --binary` invocation: the deliberate IPv4 loopback server and an additional CLI-created IPv6 wildcard listener. Both verified task processes were stopped. Supabase/LiveKit remained on loopback. With both IPv4 and IPv6 Metro connections confirmed refused, a **6,153-byte malformed mnelo:// query** cold-launched the embedded app, restored Auth and rendered Chats; tapping Me displayed Development Nika. The same warm input returned to Chats while preserving the process, and tapping Connect worked. Both OS URL dispatches returned exit 0 (0.257 seconds cold, 0.363 seconds warm); these timings measure dispatch, not full startup performance. Actual viewed native screenshots and post-input interactions establish the UI result. This closes the missing iOS simulator cold/warm check; the vulnerable decoder package is still installed.

The standalone process capture has 1,194 entries, zero targeted fatal-JS/Keychain exceptions, eight error-level entries and one fault-level entry. Retained diagnostics include UIKit's future UIScene requirement, simulator focus/accessibility/Foundation/network messages and Expo NativeResponse `currentState[6] validStates[0]`. Installed Expo Fetch source maps 6 to errorReceived and 0 to initialized; its delayed start callback logs and returns in that state. An abort/start ordering is consistent with this guard, but the capture does not independently establish the request's cause. No crash or unresponsive post-link UI occurred; this is not a zero-warning claim and no native fetch patch was applied.

After validation, the Development Client was restored, along with one explicit localhost-only Metro listener; it rendered Chats with the same authenticated session. No second CLI-started server is needed when installing the already-built app. Both simulator archives and their hashes are in [native results](audits/ios-26.6/native-result.json). Physical iPhone still reports paired, Developer Mode enabled, iOS 26.6.1 and disconnected. Push enablement shows the expected configured-build-required message; no APNs/FCM delivery or physical permissions/calls are claimed.

### Android cross-check against the same maintenance lockfile

Incremental Android prebuild passed in 2.34 seconds without source/config changes. No additional Android toolchain was installed. Current-source diagnostic build passed in **14m28s reported by Gradle / 869.55 seconds wall time**, 904 tasks (343 executed / 561 up-to-date). Development build passed in **10m45s / 647.38 seconds wall time**, 533 tasks (193 executed / 340 up-to-date). Both use source commit 14312ca, SDK 57 maintenance versions and existing isolated JDK 17/API 36 tools. Upstream Gradle/native deprecation warnings remain.

Current diagnostic APK: 60,159,989 bytes, SHA-256 `97d0c597019ef67360fe18c296780e7a3b2c5e8dd7a747f6beba3dfa6fb94ead`. Current Development Client APK: 105,371,750 bytes, SHA-256 `90aa19c69c583d91eb9b494d37c0648077605307ccc9264b61ac092704c130b8`. Paths remain artifacts/android/mnelo-local-diagnostic-arm64.apk and mnelo-development-arm64.apk. September 8 binaries were preserved under artifacts/android/2026-09-08 before replacement. Both signature and 16-KiB ZIP alignment checks pass; all 24 diagnostic native libraries have LOAD alignment at least 16 KiB. Embedded Android bundle/strings and iOS embedded bundle/strings each scan with zero secret findings. No physical page-size or store-signing claim.

The first emulator boot overlapped compilation. System logs record System UI, keyboard, Google services and phone-service ANRs **before** the Mnelo launch. The first correct activity dispatch returned exit 0 but **Status: timeout**, and the viewed screenshot showed the System UI not-responding dialog over Chats; this was not a passing launch. An earlier harness invocation had an invalid activity component qualifier (exit 255), corrected to the package/activity syntax before this test. No application or OS error was hidden. Only the two task emulators were stopped, preserving userdata; after compilation, the same Android AVD rebooted alone. The new boot's ANR/crash event log is empty.

On that quiet boot, the current embedded APK cold-launched with the 6,153-byte malformed URI, Status: ok (1.432-second dispatch); an initial loading frame was followed by a viewed authenticated Chats screen without a system dialog. The warm URI returned Status: ok in 0.069 seconds, preserved the process and rendered Chats after the transition. No Metro tunnel was present. The 210-line standalone process capture has zero targeted fatal/React Native error lines. These timings are dispatch diagnostics, not startup benchmarks. The current Development Client APK was then installed with the explicit Metro tunnel and rendered authenticated Chats. CUA did not expose the standalone emulator executable; native SDK install/launch/screenshot diagnostics were used, without claiming a fresh post-link tap or full interactive A–G regression. The older full interaction results remain dated. Physical-device and hosted distribution acceptance stay open.

Final source QA: strict TypeScript, lint, formatting, source/environment/localization guards, 164 Jest tests/32 suites, three server-stream tests, Doctor 21/21, compatibility and both mobile exports PASS. History/source/export secret scans each report zero findings. Fresh npm audit retains 16 moderate / zero high/critical. No database, application source or environment guard changed after maintenance commit 14312ca.

## September 9 — visual-system continuation

The [design pass](DESIGN_QA.md) keeps Expo SDK 57 and the existing native stack. `expo-font@57.0.3`, previously transitive, is now a direct dependency; `ios/Podfile.lock` already contained exactly that pod. The new Inter/Georgian/Feather assets load through Expo Font; no native source patch or SDK upgrade was used.

The actual Xcode 26.6 Debug simulator build finished `BUILD SUCCEEDED`, exit 0. Its complete log is `artifacts/design-ios-build.log`; metadata records 3,626 warning lines and zero error lines. The Development Client was installed on iPhone 17 Pro / iOS 26.5, loaded the revised localhost bundle, and rendered real local OTP/Chats/Connect/Me/results flows. Fonts, field caret and live text enlargement were inspected on this runtime. See [verification metadata](audits/design/verification.json) for exact evidence and limitations.

Fresh iOS/Android exports pass. Earlier simulator ZIPs and Android APKs remain pre-design snapshots; they have not been relabelled as new-design standalone builds. No physical iPhone, store, cloud build or new Android APK is claimed. The icon/splash still uses the temporary M placeholder pending the actual approved Light mark reference.

## September 9 — approved Light identity native rebuild

The source render and full design brief were retrieved. The selected vector geometry now generates the Light launcher, adaptive/monochrome Android layers, notification silhouette and light splash from authoritative Expo config. No runtime native dependency version was changed in this pass; comparing the previous and current npm lockfiles shows zero version changes among previously installed packages. Sharp 0.35.4 is the only new direct dependency and is development-only.

SDK 57 CNG regenerated both native folders; the first prebuild used its default clean behavior. The later Android-only asset adjustment was regenerated before compilation. CocoaPods 1.16.2 completed with **130 dependencies / 133 pods** using the existing Ruby 2.6 compatibility environment. This fresh install selected React Native's supported source fallback because prebuilt artifacts were unavailable. Relative to the archived Phase 30 lock, seven C++ dependency pods replace React-Core-prebuilt and ReactNativeDependencies; that accounts for the five-pod total increase. The archived lock also predates the already completed Expo patch maintenance. Current ExpoModulesCore is 57.0.17 and ExpoModulesJSI is 57.1.0. Existing Ruby `filter_map`/precompiled-config warnings remain; no generated source was patched.

Actual iOS command:

```sh
xcodebuild -workspace ios/Mnelo.xcworkspace -scheme Mnelo -configuration Debug \
  -sdk iphonesimulator -destination id=3A981CD5-4BF5-4460-9F44-64A788301505 \
  -derivedDataPath artifacts/ios-26.6-derived \
  CODE_SIGNING_ALLOWED=YES CODE_SIGN_IDENTITY=- build
```

**BUILD SUCCEEDED**, exit 0, zero compiler errors and **1,980 warning lines**. Xcode 26.6 (17F113), Simulator SDK 26.5, minimum app iOS 16.4, bundle com.mnelo.app, display name Mnelo. Ad-hoc signature verification passes. Installed on iPhone 17 Pro Simulator; Welcome/chat/keyboard/sheets and the actual App Library icon were visually inspected. `artifacts/design-complete-ios-build.log` retains native warnings. This Debug Development Client serves JavaScript from loopback Metro; it is not an IPA or a standalone preview build.

`npm run android:build` completed **BUILD SUCCESSFUL in 5m20s**, 533 tasks (501 executed, 32 up-to-date). New debug-signed ARM64 APK: `artifacts/android/mnelo-development-arm64.apk`, **105,408,299 bytes**, SHA-256 `d3a7191958d2efaa0dd6a459af35bb2c54a9331462cf63371dac75bba0fd4c33`. Signature and `zipalign -c -P 16 4` pass. Existing Google API 36 Pixel 7 emulator booted; APK installed and the new Welcome artwork actually rendered. No new Android toolchain or Google account was created. The current native UI tool did not expose the emulator as a controllable app, so this pass does not claim a fresh interactive Android regression suite or hardware QA.

The preview used a temporary, ignored development-mode public environment file to isolate existing in-memory fixtures. It was removed afterward; original `.env.local`, backend data and real mobile sessions were preserved. Final iOS/Android exports and secret scans ran again after restoration. Earlier standalone diagnostic APKs/ZIPs remain historical snapshots and are not labelled as these updated development builds. Full findings, screenshot/artifact hashes and remaining acceptance: [design QA](DESIGN_QA.md), [verification](audits/design/identity-verification.json).

## Native visual regression pass — September 9, 2026

Starting commit `4ff7f2d`; no runtime/native dependency or generated project changes. Fresh incremental Xcode 26.6 Debug simulator compilation using the command above succeeded (exit 0, zero compiler errors, **3 warning lines**). Signature verification passed. Android `npm run android:build` succeeded in **26s**, 533 tasks (19 executed, 514 up-to-date); APK signature and 16-KiB ZIP alignment checks passed. Current JS was served to both platforms by loopback Metro; these Debug builds do not embed a standalone production JS bundle. Final mobile exports are separately checked.

The iOS Georgian matching crash was a runtime API incompatibility, not a recurrence of ExpoModulesJSI compilation failure: `TypeError: undefined is not a function` at `formatNumber` calling `Intl.NumberFormat.formatToParts`, through `MatchReason`. A stale Metro session initially displayed an unrelated mapped location. Restarting with `expo start --dev-client --localhost --port 8083 --clear` produced the correct stack. The [official Hermes Intl API table](https://github.com/facebook/hermes/blob/static_h/doc/IntlAPIs.md) marks this NumberFormat API as Android-only. `format()` plus deterministic separator mapping preserves existing Georgian output without adding a polyfill/dependency. A test removing `formatToParts` reproduced the failure, then passed; actual Georgian Connect/results/profile/request navigation passed on iPhone SE after the fix.

Local Edge Functions were initially unavailable (503) because the functions serve process was absent. Restarted the repository's guarded `npm run functions:serve`; private media and local call signaling succeeded. Two controlled gateway pauses automatically resumed after 50/40 seconds. A message and photo remained pending, then completed after recovery. The message's database count was exactly one. This does not substitute for physical airplane-mode or two-participant RTC tests.

Doctor's first invocation reported 20/21 because CocoaPods was outside this shell's PATH. The existing Ruby setup (`PATH="$HOME/.gem/ruby/2.6.0/bin:$PATH" RUBYOPT=-rlogger`) produced 21/21, with dependency compatibility up to date. The native stack was not changed. Metro retains pre-fix crash evidence and development animation-listener warnings; no new fatal error was observed after retesting the fix. Android cold startup initially showed the development-client blank/loading surface while Metro compiled 2,291 modules; it then restored the real local account and rendered the populated inbox. The emulator remains unavailable to the current native UI controller, so Android interactive acceptance stays open.

Raw evidence: `artifacts/visual-qa/{ios-build,android-build,metro,functions,android-runtime,doctor,compatibility,final-check,export,secrets}.log`. [Visual QA and remaining boundaries](DESIGN_QA.md); [sanitized evidence manifest](audits/design/visual-qa-verification.json).

The final signup visual pass additionally found the full-screen country picker drawing under the iPhone clock/status area. A modal-local SafeAreaProvider fixes the actual native inset scope, as described by the library’s [official provider guidance](https://appandflow.github.io/react-native-safe-area-context/api/safe-area-provider/). No fixed status-bar padding or native patch was introduced. Search/no-results/selection and new-account identity/capability navigation were then verified.

## Physical iPhone signing after team selection — 2026-09-11

Current device-owned messenger checkout: after the owner selected their Apple team, the paired iPhone 17 Pro Max / iOS 26.6.1 reports unlocked, Developer Mode enabled and developer disk-image services available. Xcode confirms Automatically manage signing and the selected team for `com.mnelo.app`.

`xcodebuild -workspace ios/Mnelo.xcworkspace -scheme Mnelo -configuration Debug -destination 'generic/platform=iOS'` exited **65** at profile validation. The wildcard `iOS Team Provisioning Profile: *` lacks Push Notifications and `aps-environment`. Xcode's Signing & Capabilities panel additionally displays **`Unable to process request - PLA Update available`** and requests acceptance of the latest Program License Agreement. This account-level restriction blocks profile resolution; it is not evidence of a renewed ExpoModulesJSI compiler failure.

Evidence: `artifacts/iphone-20260911-team-build.log`, `artifacts/iphone-20260911-team-build.xcresult`, device details under `artifacts/iphone-selected-team-device.json`, and direct Xcode UI inspection. No successful physical build/install/boot or device acceptance is claimed. The owner's local team selection remains intact; the agent made no credential export, provisioning mutation or legal acceptance. The next exact Account Holder action and later build retry are documented in [IPHONE_DEVELOPMENT](IPHONE_DEVELOPMENT.md).

## Agreement accepted; requested App ID unavailable — 2026-09-11

After owner-confirmed agreement acceptance, repeated the generic physical iOS Debug build with `-allowProvisioningUpdates`. It exited **65**. Both Xcode and the log report `Failed Registering Bundle Identifier` because `com.mnelo.app` is unavailable to the selected team. The previous PLA error is no longer displayed. The wildcard profile's missing Push Notifications / `aps-environment` errors remain; no entitlement was removed to bypass them.

No alternate App ID was registered or configured. Inspecting the owner's actual identifier list is the next prerequisite; the agent's browser currently requires Apple sign-in. Do not infer the identifier's ownership from this error alone. Evidence: `artifacts/iphone-after-agreement-build.log`, corresponding `.xcresult`, and the Xcode Signing & Capabilities panel. Compilation/install/boot remain unproven for physical iPhone.

## Owner-approved replacement; physical iPhone build and boot — 2026-09-11

The owner reported `com.mnelo.app` absent from their Apple identifier list and explicitly approved **`com.mnelo.messenger`** for both platforms. Authoritative Expo configuration, Android launch script, store metadata and current identity documentation now use the replacement. Former identifiers remain only in explicitly historical documentation/evidence; Git history is unchanged. No existing app sandbox or account record was deleted or migrated.

`npx expo prebuild --no-install` completed, but this SDK automatically cleared/recreated both native directories for the changed identifiers. The immediate first build therefore failed at missing `ios/Mnelo.xcworkspace`, before compilation (`artifacts/messenger-identity-iphone-build.log`). Ran CocoaPods with the existing `PATH="$HOME/.gem/ruby/2.6.0/bin:$PATH" RUBYOPT=-rlogger` environment: **127 Podfile dependencies / 130 pods**, exit 0. Existing Ruby `filter_map`/precompiled-configuration warnings select the source-build path and are retained. Prebuild also removed the local team selection; restored the exact previously owner-selected team and automatic signing in both ignored generated build configurations. No generated native source hack or dependency update was used.

Successful command:

```sh
xcodebuild -workspace ios/Mnelo.xcworkspace -scheme Mnelo \
  -configuration Debug -destination 'generic/platform=iOS' \
  -destination-timeout 15 -allowProvisioningUpdates \
  -derivedDataPath artifacts/iphone-20260911-build \
  -resultBundlePath artifacts/messenger-identity-iphone-provisioned.xcresult build
```

**BUILD SUCCEEDED, exit 0**, Xcode **26.6 (17F113)**, zero compiler errors, **2,141 warning lines** in the complete log. ExpoModulesJSI compiled successfully. `codesign --verify --deep --strict` passes on the resulting `Debug-iphoneos/Mnelo.app`. Bundle ID/display name are `com.mnelo.messenger` / Mnelo. Apple issued the explicit `iOS Team Provisioning Profile: com.mnelo.messenger`, with `aps-environment=development`, debug entitlement and the actual connected iPhone included; expiration 2027-09-11. Only sanitized metadata is committed; private profiles, device identifiers and signing credentials remain outside Git.

`devicectl device install app` exited 0. The console-attached launch of `com.mnelo.messenger` with its `mnelo://expo-development-client/` URL succeeded on **iPhone 17 Pro Max / iOS 26.6.1**. LAN Metro returned the manifest/bundle with HTTP 200; the bundle download was 11,303,407 bytes. The physical app loaded JavaScript and an Xcode device screenshot was captured and visually inspected: **Mnelo Welcome rendered**, with the approved mark, Create on this device and Restore a backup. The development-client Tools overlay is visible. This is not simulator evidence and not an IPA/TestFlight build.

Initial runtime log contains the Expo/native background-fetch and remote-notification delegate warnings, Google logging initialization and a dropped `topMomentumScrollEnd` event. No fatal/unhandled JavaScript error was observed in this initial capture. Required background/call/push behavior remains untested; capabilities were not expanded just to remove warnings. The scoped Metro process deliberately has no relay or phone identity endpoint, preserving `.env.local`; it supports offline UI inspection without exposing the loopback-only fixture. SMS/provider activation, physical registration, two-peer messaging/media/calls and full interactive acceptance remain pending.

Fresh mobile exports pass for iOS and Android; fresh export/history/source secret scans each have zero findings. Full source suite passes (222 tests). Doctor **20/21** and `expo install --check` report 25 newly recommended maintenance patches; installed Expo **57.0.21** bundled compatibility ranges accept all **36** checked installed dependencies. This scoped identity/signing change keeps the existing lockfile; it does not claim current remote compatibility check PASS. No new Android APK is attributed to this identifier. Evidence: ignored `artifacts/messenger-identity-*` logs/results and [sanitized verification](audits/iphone-identity/verification.json).

## 2026-09-11 — Local attention native verification

Current JS was verified in the existing iPhone 17 Pro / iOS 26.5 development client. iOS/Android Hermes exports pass; no native dependency/configuration changed and no fresh Xcode/Gradle compile is claimed. Native Expo Notifications permission and generic local banner presentation both worked. No remote push token or SMS operation was requested.

Native fixture creation exposed `SQLiteErrorException: Error code 19: NOT NULL constraint failed: group_deliveries.revision`. The existing simulator table lacks the default present in the current schema. All engine group-delivery inserts now explicitly pass revision 0. Re-running group creation in that same database succeeded without reset/migration. A real SQLite regression test recreates the legacy no-default shape and validates creation/membership update.

Hot reload also exposed cached `IDENTITY_REQUIRED` / `AccessClosedResourceException: Access to closed resource` errors from a replaced engine/closed SQLite connection. DeviceProvider now invalidates device queries when the initialized engine changes. Native query-cache inspection after the fix returned no errors, and the existing history continued to render. This does not suppress SQLite errors or replace a missing encryption key.

Doctor initially returned 19/21 in the shell without CocoaPods on PATH. The repository's existing `PATH="$HOME/.gem/ruby/2.6.0/bin:$PATH" RUBYOPT=-rlogger` environment returned 20/21; only the already-known 25 Expo patch recommendations remain. Dependency versions are unchanged. See [current QA](QA_REPORT.md) and [notification limitations](NOTIFICATIONS.md).

## Phone onboarding runtime validation — 2026-09-11

No native module/dependency/identifier changes or native compilation were needed. The existing development binary (`com.mnelo.app`, a historical simulator installation; source configuration remains `com.mnelo.messenger`) was installed on a new iPhone 17 Pro / iOS 26.5 simulator for isolated first-entry tests. Actual native views demonstrated the inline country selector, invalid/correct fictional OTP, feature-route guard, local profile save, changed number and a cold reopen after the fictional identity service stopped.

A development-environment mismatch was investigated rather than labelled an SMS PASS. Runtime `configuredPhoneService()` initially resolved saved port 8086 while the process environment said 8087. The installed `@expo/metro-config/build/transform-worker/transform-worker.js` virtual-env implementation merges `.env`, `.env.development`, `.env.local`, `.env.development.local` over process values; its development branch does not consult the CLI's `EXPO_NO_DOTENV`. Clearing Metro did not change this. One reserved-fictional-number send action therefore reached the real local service and failed. No successful real-provider send or carrier receipt is claimed; the real registration database remained empty. A temporary ignored development override selected 8087, confirmed inside the running client, before successful fixture verification. The override and fixture service were removed afterward; normal Metro/Infobip configuration was restored. No provider secret was exposed or copied into the client.

QA captures and redacted state evidence are in ignored `artifacts/onboarding-*`. The ordinary and accessibility-medium Georgian profile fields showed no vertical glyph clipping. This is native Simulator runtime QA, not a new signed build, Android-device test, physical-iPhone OTP/autofill test or complete accessibility audit.

## 2026-09-11 — Welcome and minimal phone entry

JS/asset-reference changes only; existing approved brand exports reused. iOS and Android Hermes exports succeed. Actual development-client launch/entry checked on iPhone 17 Pro and a separately created iPhone SE (3rd generation), both iOS 26.5, using the previously compiled simulator binary. The mark precedes the form automatically. Keyboard, privacy modal and Georgian accessibility-medium text checked; a long Georgian modal heading was reduced to the shared headline style, retaining dynamic sizing and scroll access. No native rebuild or physical-device SMS claim. Configured identity/signing, original local history and real provider service are unchanged. The disposable simulator was deleted after QA. Doctor reports the existing 25 Expo patch recommendations (20/21 checks). See QA_REPORT and ignored `artifacts/welcome-*` evidence.

## 2026-09-11 — Two-iPhone TestFlight prerequisites

Xcode 26.6 (17F113), one paired/available iPhone and existing development signing remain usable. No local Apple Distribution identity was found; Xcode-managed store signing and App Store Connect access remain unverified. Owner Apple authentication completed; Mnelo app record 6811153275 now exists and TestFlight is empty. EAS submit pins that app ID; no new App ID or uploaded binary. EAS CLI 23.2.0 reports Not logged in, but local Xcode distribution does not require an EAS login. No build was started with known loopback-only phone/signaling addresses or by bypassing the protocol release gate. Preflight exit 1 records prerequisites, not an Xcode compilation failure. No new archive/IPA/build-upload ID or second-device PASS. Hetzner account authentication and later billing approval are pending for a proposed development VM; no server order or DNS change was made. See TWO_IPHONE_TESTING.md for the current sequence and QA_REPORT.md for executed checks.

## Development hosting checkpoint — 2026-09-11

The owner authorized a small rented server for up to approximately 50 testers. Hetzner currently requires owner login/registration; no order, VM, DNS record or SSH/secret transfer has been performed. The proposed CX23 EU configuration and exact deployment prerequisites are in [DEVELOPMENT_HOSTING](DEVELOPMENT_HOSTING.md). This does not change the mobile release gate or claim a new native build.

The isolated 50-peer local signaling/reconnect check passed, and the full check suite passed with 291 tests. This validates local routing only; remote host, TURN bandwidth and two-phone media/SMS acceptance are still untested. See the matching entry in [QA_REPORT](QA_REPORT.md).

## Hosted development backend — 2026-09-12

Provisioned one Ubuntu 24.04.5 CX23 in Nuremberg under Mnelo Development: `mnelo-dev-01`, 165553002, IPv4 46.225.169.127; $7.09/month excluding tax including IPv4. No backups/snapshots/additional paid resources. Installed SHA-256-checked official Node 24.18.0 (npm 11.16.0), Caddy 2.11.4 and explicit bundled relay/identity runtimes; no full repository, mobile vault or legacy backend uploaded. One admitted Infobip trial recipient; read-only provider setup passes on VM, no SMS sent.

Actual cloud-init schema validation, security-update reboot, SSH key-only access and port/isolation inspections pass. Fixed a concrete vendor journald ordering problem by moving the override after syslog.conf; disabled rsyslog/UFW logs, set kernel core pattern /dev/null, zero swap/core limits, discarded Caddy/process logs and removed two bootstrap-only persistent journals. Fresh persistent journal/crash/core scans were empty. SSH bootstrap was TOFU to the console-assigned IP, pinned thereafter; no independent console fingerprint verification is claimed.

Relay synthetic checks pass on the actual VM/service through WSS (50 simultaneous routes, 50 signed offers, invalid signature, reconnect and cleanup), also after restart. Identity HTTPS checks pass signed status, rejected unregistered lookup/forged proof/origin/content endpoint and proxy-header quota bypass. The first Mac WSS probe reached the stale Vercel wildcard/404; authoritative/public DNS and VM hostname checks resolve correctly, and Mac TLS validates when addressed directly with the correct hostname. This is retained as a DNS failure, not hidden as native or WSS success.

Full local QA: 298 tests and strict checks pass. Doctor with existing Ruby/CocoaPods PATH workaround: 20/21; the only failure is the previously documented 25 Expo patch recommendations. Compatibility also returns exit 1. No compiler, native project or app identity change and no new iOS/Android binary was attempted in this server checkpoint. TURN, app endpoint/binary validation, real OTP/media, distribution and independent protocol review remain required. See DEVELOPMENT_HOSTING.md and TWO_IPHONE_TESTING.md.

## Funded SMS and hosted development handoff — 2026-09-12

Owner completed Infobip top-up: live portal confirms $20 available and pay-as-you-go enabled, with 15 free messages remaining and both automatic-recharge options unconfigured. The two owner-selected tester indexes replace the unused initial admission; root configuration stays 0600. The admission updater preserves the HMAC key, provider settings, registry and durable budget; zero registrations and zero reservations verified. No SMS was sent. A first operator wrapper failed Python syntax validation before execution; corrected it and precompiled it before the successful update.

The Mac first still resolved the relay through its cached Vercel wildcard and the WSS check failed. After the OS cache expired, `dns.lookup` returned 46.225.169.127 and the complete WSS/HTTPS checks passed from the Mac with normal DNS/TLS. No resolver or certificate bypass. The six deployed service/check bundles were matched against clean commit f03270619262e3da75dcf5f8c858788361223374 before synchronizing their provenance manifest.

Prepared an ignored development-only override selecting the hosted endpoints and restarted Metro on 8083. The existing signed com.mnelo.messenger development client is installed on the paired iPhone. Its launch was denied solely because iOS reported the device locked (CoreDevice 10002; FBSOpenApplicationErrorDomain 7). No app-screen rendering, OTP, new native compilation or store build PASS is implied. User unlock is the next physical action; TestFlight/Internet media prerequisites remain open.

Owner unlocked the phone; the next launch succeeded. The owner then explicitly confirmed that SMS arrived and the code was accepted. The Xcode screenshot shows Chats, and read-only hosted state shows **one registered identity, hour used=1, day used=1**. Infobip now shows 14 of 15 free messages and $20 balance. No OTP value was requested/read. An optional unauthenticated Hermes inspector probe returned 401 and was not bypassed; it was unnecessary after the owner confirmed registration and the backend corroborated it. No session/key/history was erased or rewritten.

The second iPhone is remote; continue with TestFlight preparation rather than requiring local USB access. This checkpoint verifies one physical registration, not two-phone messaging/media or a store build. The debug bundle scan surfaced an unused legacy Supabase **publishable** key and a React Fast Refresh signature as generic-key matches; neither is a privileged secret. They were classified explicitly, not suppressed or counted as a clean raw debug scan. Source/history/existing exported mobile bundles and all new hosting bundles scanned with zero findings.

The remote-tester TestFlight profile now validates against the installed EAS schema: store distribution, Release, preview backend, automatic build-number increments, existing Apple target. Its preflight returns only MESSENGER_SECURITY_REVIEW_REQUIRED; selecting the development-client profile is correctly rejected for store upload. Full local QA passes 302 tests. This removes a configuration gap without changing the release gate, signing, phone identity or vault. No native build/upload was attempted or claimed. Deployed runtime plus operator helper match clean hosting commit 48eb170f900c49fe16348ff653e66655976fcbdb.

## 2026-09-12 — coturn and standalone iOS beta

Ubuntu's coturn candidate was 4.6.1-1build4, behind upstream security fixes. Built official 4.18.0 at 23c6c1d32a3d2b21a56cee65d3203bcc4ea82d2b on the existing CX23; no new server or paid add-on. CMake initially required libmicrohttpd-dev: 4.18 always compiles metrics support, while its listener remains disabled by default. Installed that declared build dependency. Effective compile flags disable SQLite/PostgreSQL/MySQL/Mongo/Redis/SCTP/systemd integration. Runtime installed separately under /opt/coturn-4.18.0. Initial service startup failed on directory permissions, corrected using a separate root:mnelo-turn 0750 config directory and 0640 config; the identity/provider directory remains root-only. Runtime is active with public TCP/UDP 3478 and UDP 49160–49759, capped allocations/bandwidth and no content persistence.

Expo 57 maintenance alignment initially exited 1 because dynamic app.config.ts cannot be auto-edited; dependencies had installed. Added the requested expo-font and expo-sharing config plugins. Sharing inbound extensions remain disabled by default; no new App Group or extension identity created. Compatibility then PASS and Doctor 21/21 with existing CocoaPods environment. Exact core stack: Expo 57.0.22, React Native 0.86.3, React 19.2.3, Router 57.0.21, dev client 57.0.19, ExpoModulesCore 57.0.18, ExpoModulesJSI 57.1.0, Xcode 26.6 (17F113).

Standalone configuration is preview with isolated hosted services, Mnelo 0.1.0 build 2, com.mnelo.messenger, team CS6GJ2BMS9. The first export attempt detected a profile/environment mismatch before bundling; explicit build-profile environment and the private production-mode beta dotenv resolved it. iOS/Android Hermes exports completed with zero secret findings. Clean native prebuild and pod installation completed. Signed Release archive at artifacts/Mnelo-0.1.0-2.xcarchive is being built; success/upload are not asserted at this checkpoint.

### Standalone archive, export, upload and first-device installation result

Source commit: c2d2f05608ef50124baa934976869bacb88b2d92. Xcode 26.6 Release archive **PASS**; Apple-managed App Store distribution export **PASS**; upload **PASS** (Uploaded Mnelo / EXPORT SUCCEEDED). Upload logs confirm App Store ID 6811153275. Artifact: artifacts/Mnelo-0.1.0-2.xcarchive; distribution IPA: artifacts/Mnelo-beta-export/Mnelo.ipa (25,935,868 bytes; SHA-256 5fad5f6c9e2d35a33ebddfe0a481aaa182c2fb777064cdbe401672664fe48d40). Native app signature verification passes; embedded JS contains the intended hosted endpoints and excludes the old public Supabase key. Native bundle secret scan: zero findings.

Non-fatal Apple upload warnings: missing vendor dSYM files for React.framework (EE15B752-FB46-35F2-B0BB-A46636572385), ReactNativeDependencies.framework (DB4E1216-70C6-36F0-8A46-FDD48FCAE1F2), WebRTC.framework (4C4C4475-5555-3144-A13A-1DDFE803BC67) and hermesvm.framework (BE274A23-47CC-3F5A-9E98-8474553DF5A8). The installed Hermes release archive does not include a dSYM. These reduce native framework crash symbolication; no fabricated replacement symbols or compiler/error suppression was used. App archive/upload succeeds despite these warnings.

The existing first iPhone reconnected over its paired network. In-place installation of the development-signed standalone archive application **PASS**; direct launch without a Metro URL **PASS**, and the launched process remained present in a subsequent device query. Installed version/build confirmed as 0.1.0 (2). Old/new application identifier, team identifier and keychain-access groups match. No uninstall, key reset, OTP send or account unlink occurred. Xcode's Devices UI remained stale/disconnected despite successful CoreDevice commands, so a new screenshot/post-upgrade enrollment rendering is **not** claimed. Physical visual/media/call acceptance remains for the owner.

Hosted identity code deployed from the same clean commit after verifying all seven runtime hashes; original index key preserved and registry count remains 1. SMS counters remain hour=1/day=1 (the original owner-initiated registration). Live HTTPS checks deny unregistered TURN/lookup and forged proofs; WSS 50-peer/reconnect checks PASS.

## September 12 — background candidate 0.1.0 (3)

Owner requires native background alerts/calls on iOS and Android. Added local Expo module MneloCalls: PushKit/CallKit/AppDelegate and Core-Telecom 1.0.0/Firebase Messaging 25.0.1. Initial Swift actor-isolation errors were fixed with async MainActor execution; Kotlin HeadlessJsTask/Coroutine references through the React dependency and correct Expo import. No permanent generated-code hacks.

Core-Telecom requires minimum API 26: Expo-compatible expo-build-properties 57.0.17 sets app minimum and module Gradle agrees. Android prebuild PASS. A build guard stopped below its free-space floor; only this task's old regenerable simulator derived cache was removed, preserving device state, archives/IPAs/logs and other projects. Final Gradle build **PASS in 40s**, 596 tasks (29 executed, 567 up-to-date), existing JDK 17/SDK 36. APK verifies com.mnelo.messenger, Mnelo, min 26 / target 36. This is debug-signed ARM64 development client, not a standalone Play artifact.

Xcode 26.6 final Release archive **PASS**, App Store export **PASS** at 03:30 Tbilisi. Archive artifacts/Mnelo-0.1.0-3.xcarchive; IPA artifacts/Mnelo-0.1.0-3-export/Mnelo.ipa: 26,016,537 bytes, SHA-256 `58c5e083c50ebc6889161cf8e8210684bd83f88c115b7dcde8d1c38a0da7162d`. Min iOS 16.4, original app/team; UIBackgroundModes audio/voip/remote-notification. Archive development signature supports local installation; exported signature is distribution with aps-environment=production/get-task-allow=false. Both verified. Final JS source modification preceded bundle timestamp. No ExpoModulesJSI blocker recurred.

Android artifacts/android/mnelo-development-arm64.apk: 114,673,253 bytes, SHA-256 `f7b3c829436cb87224fa267eed60cdc26f06140ca1ea15a252e3f50ad82ffb38`. Doctor 21/21, compatibility, both exports, secrets checks and 325 tests PASS. Native Hermes printable-string scan zero findings; not a whole-framework security audit.

First iPhone in-place build-3 install **PASS** at 03:41; phone is locked and unlocked launch/vault migration/visual retention remain pending. No reset/uninstall/new SMS. Apple upload **PASS** at 03:42:51; artifacts/background-ios-upload.log records success and four missing vendor dSYMs (React, ReactNativeDependencies, WebRTC, hermesvm). Apple processing, compliance assignment and external testing eligibility are not proven by upload. Production APNs owner download and Firebase owner terms/configuration remain external prerequisites. See PUSH_OPERATIONS and QA_REPORT.

Build-3 upload receipt: **6efc5bee-6a17-4cb3-a093-dd4b51d7b330**; App Store Connect still displays No Builds at the latest refreshed check. CoreDevice independently confirms installed 0.1.0 (3) on the original phone; unlocked launch remains pending. Hosted background code `5b8197a97ea25508ae70caa3a4e776e0652f38d5` is active after correcting systemd's protected 0440 credential-mount handling. Full suite now **329 PASS**, live HTTPS identity and 50-peer WSS checks PASS. Sandbox credential only; Production download/Firebase setup and actual device wake/audio tests remain open.

## September 12 — Production push credential and Firebase project follow-up

Production APNs key UHS939BXXT downloaded successfully through native Chrome, validated as EC P-256 and deployed through a separate systemd LoadCredential. Scope is only com.mnelo.messenger / Production; Sandbox remains unchanged. Existing identity/SMS budget/key/admission and push state was verified preserved. Identity, relay, mnelo-turn and Caddy remain active; public no-SMS HTTPS authorization check PASS. This changes server configuration only, with no new archive, IPA, upload or Android binary.

Firebase terms acceptance was confirmed by the owner. The in-app creation error concealed a partially created Cloud project; Chrome inventory found mnelo-development and completed Add Firebase with Analytics off. Project number: 657690934155. No new billing/paid service was enabled. Android app/client configuration and least-privilege sender credentials are incomplete; Google Cloud sender creation failed in the in-app browser (tracking c50707889250639), and the refreshed list shows only the default Firebase Admin SDK account with no keys. Chrome later ceased returning window controls to automation. Do not infer Android FCM readiness from the existing APK.

At 12:26:58 Tbilisi CoreDevice reports passcodeRequired=false and unlockedSinceBoot=true for the original iPhone. At 12:29:12, launching the existing com.mnelo.messenger installation **PASS**. No reinstall/reset/OTP or key/database extraction. Visual history retention, alert permission, real receipt/call/media and second-phone acceptance remain unverified. One Sandbox VoIP route and zero peer grants are present; no alert route is registered. Refreshed TestFlight still asks to submit a build; successful upload remains separate from proven beta availability.

Evidence: artifacts/apns-production-deployment.json, push-followup-iphone-lock.json and push-followup-iphone-launch.json. Private key bytes and token values are excluded from reports.

Follow-up liveness query could not locate the previously connected phone (CoreDevice error 1011); this is a device-connection failure, not evidence of an app crash or continued runtime. Preserve the successful launch result without claiming visual/device-flow acceptance. Documentation formatting, source-security guard and Gitleaks history/source/existing mobile exports pass; no new native compilation was run for this configuration-only update.

## September 12 — TestFlight ITMS-90683 diagnosis and build 4

Phase: TestFlight delivery repair.
Status: build 3 rejection resolved in build 4; Apple processing and first-phone internal TestFlight opening confirmed. External review access and two-phone physical acceptance remain open.
Implemented: restore the required Motion purpose string through the supported Expo Location plugin, with EN/KA translations. Add `npm run check:ios:permissions` to inspect the actual generated/archive application. Increment iOS build to 4; native identity and existing device keychain/vault stay unchanged.
Files: app.config.ts, src/i18n/native/en.json, src/i18n/native/ka.json, package.json, scripts/check-ios-permissions.mjs and delivery documentation.
Database migrations: none.
Security impact: no new motion API use, permission request, data collection, server change or device reset. Android motion recognition remains explicitly disabled.

Evidence: the owner-authorized Mail inspection found Apple's 03:43 Tbilisi email for App 6811153275, version 0.1.0, build 3: **ITMS-90683**, missing **NSMotionUsageDescription** in Mnelo.app. The email explicitly requires this key even for APIs referenced only by an external SDK. Upload success at 03:42:51 was followed by rejection; it was not proof of successful processing. This supersedes earlier processing-unconfirmed statements.

Installed `expo-location` 57.0.17 links `CMMotionActivityManager` in `ios/LocationModule.swift`, `Requesters/MotionActivityPermissionRequester.swift` and `Providers/MotionActivityStreamer.swift`. Its `plugin/src/withLocation.ts` says `motionUsagePermission: false` omits the purpose string; it does not remove native APIs. That option was set to false in this repository. Reanimated 4.5.1 also contains CMMotionManager sensor support. A search of active app/src/modules found no MotionActivity, useAnimatedSensor, Pedometer or requestMotion calls. No unsupported native stripping/patch or invented motion feature was introduced. The localized fallback truthfully says Mnelo does not use motion/fitness data and denial does not disable messaging/calls.

Tests: the new artifact check **correctly rejects the preserved build 3 archive** with IOS_PURPOSE_MISSING: NSMotionUsageDescription; regenerated iOS configuration passes all six required purpose strings and EN/KA checks. The guard also rejects unintended always/background location configuration. Initial guard development exposed the generated locale path under Mnelo/Supporting; it now explicitly distinguishes generated sources from the final .app layout.
Native checks: incremental Expo `prebuild --no-clean --no-install --platform ios` PASS, using the existing testflight/preview profile and EXPO_NO_DOTENV=1. No dependency versions changed; existing Pods retained.
Known limitations: external review access, beta approval and two-iPhone background receipt/audio acceptance remain incomplete. Apple processing is confirmed below.
Commit: recorded by the delivery repair commit after verification.

Before every upload run `npm run check:ios:permissions -- artifacts/Mnelo-0.1.0-4.xcarchive/Products/Applications/Mnelo.app` (substitute the actual new archive path). Run the same check against the exported Payload/Mnelo.app. Passing this preflight never replaces Apple processing or device QA.

References: [Apple protected-resource declarations](https://developer.apple.com/documentation/uikit/protecting-the-user-s-privacy/requesting-access-to-protected-resources), [Expo Location configuration](https://docs.expo.dev/versions/latest/sdk/location/).

Build 4 local result: signed Release archive **PASS**, App Store Connect distribution export **PASS**. `artifacts/Mnelo-0.1.0-4-export/Mnelo.ipa`: **26,016,890 bytes**, SHA-256 `7d9f17187816a5fd202187a1c0cd04f19754a5b0c4f7adf93be759e9a8275c6d`. Final archive and exported Payload both pass all six purpose strings and exact EN/KA translations. Exported entitlements verify `aps-environment=production`, `get-task-allow=false`, team CS6GJ2BMS9 / com.mnelo.messenger. Sixteen embedded privacy manifests parse successfully. Release strips the development-only Bonjour/local-network purpose keys. Hosted HTTPS/WSS endpoints are embedded; the obsolete local Supabase public key is absent. Native Hermes printable secret scan: zero findings. Both source/mobile exports and full automated suite pass as detailed in QA_REPORT. Existing upstream compiler warnings remain; none were suppressed.

TestFlight group preparation: created one internal group, **Mnelo Development**, with automatic distribution disabled. At creation it had zero testers and zero builds. The owner supplied the second tester's email for a private external invitation; no developer-team access or public link was created.

Apple delivery result: build 4 upload **PASS** at **13:05:55 Tbilisi**, delivery/build ID `57da4fb6-155b-46fd-9667-ce705fd8a5e5`. A fresh App Store Connect TestFlight page confirms upload **Complete** and lists 0.1.0 (4). It separately confirms builds 2 and 3 **Failed**. Build 4 initially shows Missing Compliance, which is a distribution prerequisite rather than native processing failure. The four existing missing vendor dSYM upload warnings remain (React, ReactNativeDependencies, WebRTC, Hermes); no new error was hidden. The old tab became unresponsive after an unsaved Test Information dialog, so verification used a fresh in-app-browser tab with the same authenticated account.

Build-specific compliance: reused the already owner-authorized standard-encryption answer and France No. Missing Compliance cleared; build 4 became Ready to Submit. Added only build 4 to Mnelo Development and invited the existing Account Holder. App Store Connect confirms **1 Tester / 1 Build**, tester status **Invited**. No new App Store Connect team member/role was created.

Physical follow-up: the owner confirmed the TestFlight app opened, then enabled Me → Notifications / Allow. At **13:19:53 Tbilisi**, read-only hosted counts show one registered identity, one iOS Production alert route, one iOS Production VoIP route and zero peer grants. No tokens, private phone indices, keys or vault data were printed. Registration is not proof of notification delivery or CallKit ringing. Evidence: `artifacts/build4-push-registration.json`.

External distribution: Mnelo Preview contains the one owner-authorized external tester with zero builds / No Builds Available. Selecting build 4 reached required Test Information and sign-in fields. The current service has no shared reviewer account and admits only two real numbers. No review was submitted, admission broadened, or team role/public link added. TESTFLIGHT_REVIEW_DRAFT preserves accurate metadata. The owner subsequently authorized preparing separate, isolated reviewer accounts; that is the next engineering step, not a completed deployment in this repair commit.

## September 12 — isolated reviewer access and build 5

Phase: prepare external TestFlight review without sharing real tester accounts.
Status: code/local QA/hosted deployment/archive/export/upload PASS; Apple processing and review submission pending at this checkpoint.
Implemented: explicit masked access-key sign-in for two reserved reviewer numbers, server-enforced cohort isolation, and guarded combined identity/relay deployment. Normal six-digit Infobip enrollment is unchanged.
Files: see APPLE_REVIEW_ACCESS.md and commit `ef2894d70a18948527b7d6a8f19fcb45132d7588`.
Database migrations: none; existing metadata schema preserved.
Security impact: reviewers cannot look up, signal, wake or contact the real cohort. Private 128-bit review keys have a 14-day lifetime; server receives hashes only. No credential is embedded in the client or committed.
Tests: 340 full-suite tests PASS, TypeScript/lint/format/security/env/brand/localization PASS; Doctor 21/21, compatible dependencies, both mobile exports and Gitleaks source/history/exports PASS. Hosted two-account enrollment, lookup, TURN, bidirectional signed message/call signaling and self-unlink PASS. Generic HTTPS/WSS negative checks PASS. No physical call/message/push delivery is claimed.
Native checks: signed Release archive and App Store distribution export PASS for 0.1.0 (5), com.mnelo.messenger, unchanged team CS6GJ2BMS9. Archive and final IPA permission strings/EN+KA pass. Final codesign verification, Production push entitlement, get-task-allow=false, 16 embedded privacy manifests and expected hosted endpoints verified. Native Hermes printable secret scan has zero findings.
Artifact: `artifacts/Mnelo-0.1.0-5.xcarchive`; `artifacts/Mnelo-0.1.0-5-export/Mnelo.ipa`, 26,018,640 bytes, SHA-256 `b9e2050857a7aef5cdde387cf90cf1cfe67947f17c59b629c14c53d9c2cc0299`. Upload completed at 14:07:07 Tbilisi. Apple delivery ID `fc21f388-12f0-4c99-9f92-5467fb617612`, initially Processing. The four existing missing vendor dSYM warnings remain; no error was suppressed.
Deployment: same development VM/URLs, runtime commit ef2894d, identity+relay active in one process, old standalone relay disabled, Caddy/TURN unchanged. Existing identity/SMS/push state and private keys were verified preserved; synthetic reviewer state was removed with authenticated self-unlink.
Known limitations: external Beta App Review, second-phone installation, two-phone real delivery/audio/media acceptance, Android FCM completion and independent security audit remain open.
Commit: implementation ef2894d; subsequent delivery checkpoint recorded separately.

Build 5 final Apple checkpoint (approximately 14:16 Tbilisi): **upload processing Complete**, owner-authorized compliance answers applied, actual private reviewer credentials and instructions saved, **external Beta App Review submitted**. Build `fc21f388-12f0-4c99-9f92-5467fb617612` is assigned to Mnelo Development (one internal tester) and Mnelo Preview (one external tester). The external group shows **Waiting for Review** with build 5, and **Automatically notify testers** was checked during submission. No external email delivery, approval, second-phone installation, or physical two-phone PASS is claimed. First-phone build 4 remains the last owner-confirmed installation. Reviewer implementation commit: ef2894d; this delivery evidence is committed separately.

## September 12 — build 6 distribution of profile/QR and notification defaults

Source: `a26a1a5` (includes `a8e110c`). Identity unchanged: `com.mnelo.messenger`, team `CS6GJ2BMS9`, version 0.1.0 (6). Xcode 26.6 (17F113); Expo 57.0.22/RN 0.86.3 retained.

Explicit standalone environment: `EAS_BUILD_PROFILE=testflight`, `NODE_ENV=production`, `EXPO_NO_DOTENV=1`, preview environment with hosted identity/relay. No local QA fixture, SMS secret, service token or Apple credential in the configuration. Native prebuild and CocoaPods install PASS. Xcode automatic signing Release archive PASS using the existing Apple account/App ID. Export PASS with production APNs, both mnelo.com/www associated domains, correct application identifier, and get-task-allow=false. No duplicate App ID, manually patched native code or EAS account required.

Artifact: `artifacts/Mnelo-0.1.0-6.xcarchive`; exported `artifacts/Mnelo-0.1.0-6-export/Mnelo.ipa`, **27,232,360 bytes**, SHA-256 `e096ab19c250187965cbf7fd06cad318f38e2190a16286b014e0e5bdabaeaa20`. Permissions preflight on archive AND exported Payload: all 6 keys and EN/KA translations PASS; no always/background location. Archived signature verification PASS. Compiled Hermes contains the hosted endpoints and excludes the isolated fixture/LAN values; extracted compiled-JS strings secret scan reports zero findings.

Upload PASS at approximately 17:16 Tbilisi. Apple delivery/build ID **85a1a06f-adb5-4a2b-b1c9-e0e8d68222db**; refreshed App Store Connect confirms build 6 Processing. Existing build 5 remains Waiting for Review; no cancellation or tester removal. Four existing vendor dSYM warnings remain (React, ReactNativeDependencies, WebRTC, hermesvm); app upload succeeds, but native vendor crash symbolication is incomplete.

QA at this exact source: full `npm run check` PASS — 53 Jest suites/283 tests, 3 server utility tests, 98 device/relay/protocol tests, **384 total**. TypeScript, lint, formatting, security/env/localization and 14 brand-asset checks PASS. Expo Doctor 21/21 PASS; dependency compatibility up to date. History/source/iOS+Android bundle Gitleaks checks zero findings. Android source/export from the same unchanged source retains the preceding PASS; no new Android native artifact produced by this iOS distribution task.

Disk preparation removed only named regenerable native compiler/module/index caches after low-space detection. Existing archives, IPAs, application vaults, source and earlier build evidence retained. Logs: build6-prebuild.log, build6-pods.log, build6-archive.log, build6-export.log, build6-upload.log, build6-check.log, build6-doctor.log, build6-compatibility.log, build6-secrets.log, build6-bundle-secrets.log. No physical build-6 receipt/call/camera/QR/migration PASS is claimed.

Build 6 final Apple checkpoint (17:27 Tbilisi): processing **Complete**. Reused the owner's previously authorized answers: standard encryption in addition to the operating system, France No; no new non-encryption/exemption claim added to Info.plist. Missing Compliance cleared. Saved the build-specific What to Test notes and added existing **Mnelo Development — Internal, 1 tester**. Apple explicitly disables the Mnelo Preview external checkbox while build 5 of version 0.1.0 is pending review: only one build of a version can be submitted at a time. Build 5 was not cancelled/expired and the second tester was not removed. Next external step, once Apple approves build 5: add build 6 to the existing Mnelo Preview group and complete any offered beta submission. Internal delivery is prepared; physical installation/email receipt is not claimed.

### September 12 — typography/navigation runtime validation

Presentation-only refinement uses existing expo-font 57.0.4 and six original bundled DM Sans/FiraGO faces. Xcode/native dependencies and bundle ID `com.mnelo.messenger` are unchanged. The already-built build-6 Simulator development client cold-booted the changed JS/assets, restored its isolated fixture identity and rendered three selected-tab states. Native screenshots and font provenance are retained in `artifacts/design-*` and `docs/audits/ui-fonts.json`. This is a Simulator runtime pass, not a new Xcode compilation, physical test or TestFlight upload. Both current iOS/Android Metro/Hermes exports pass. Doctor passes 21/21 using the repository’s documented CocoaPods environment; initial missing-PATH failure is preserved. See the current QA report.

## September 12 — typography/navigation TestFlight build 7

Phase: owner-requested distribution of the latest app design.
Status: signed Release archive, App Store export and upload PASS; Apple upload table confirms Processing.
Implemented: package the previously tested `353d5c7` typography/tab-navigation changes as 0.1.0 (7). Identity remains `com.mnelo.messenger`, team `CS6GJ2BMS9`; no keychain, database, enrollment service or protocol migration.
Files: app.config.ts build number; this build evidence and QA/release notes.
Database migrations: none.
Security impact: original encrypted vault, hosted preview endpoints, release gate and isolated test admission preserved. EXPO_NO_DOTENV=1 and explicit testflight/preview environment used for prebuild and archive. No production public-release claim.
Tests: full npm run check PASS: 53 Jest suites/283 tests, 3 server utility tests, 98 device/protocol tests = 384. TypeScript, lint, formatting, security/environment/localization and brand checks PASS. Expo Doctor 21/21; compatibility up to date.
Native checks: prebuild + CocoaPods PASS; Xcode 26.6 Release archive PASS; export and upload PASS. Archive and actual exported Payload both pass all six iOS purpose strings/EN+KA checks. Exported code signature PASS, production APNs, get-task-allow=false, exact identity/team and both mnelo.com associated domains verified. Compiled Hermes includes hosted endpoints and new DM Sans/FiraGO asset references; printed bundle scan zero Gitleaks findings.
Artifact: artifacts/Mnelo-0.1.0-7.xcarchive; artifacts/Mnelo-0.1.0-7-export/Mnelo.ipa, 27,851,601 bytes, SHA-256 `2329554cdf27b3ea036123905f54ce24af86167fe409520dde890a096be0744d`.
Apple build ID: `a811a289-d128-4966-8b62-2c7a9e9bbde9`, upload September 12 at 18:37 Tbilisi.
Known limitations: the existing four vendor framework dSYM warnings remain (React, ReactNativeDependencies, WebRTC, hermesvm). Native vendor crash symbolication is incomplete. No physical build-7 installation, retention, message/call/media/notification PASS is claimed. Apple processing/compliance/group delivery are separate gates. Build 5’s external review was not cancelled or replaced.
Commit: build reservation and evidence committed with this distribution task; subsequent Apple checkpoint recorded below.

Build 7 Apple delivery checkpoint: Processing completed. Reused the already-authorized standard-encryption and France No answers; Missing Compliance cleared. Saved build-specific What to Test notes and added existing **Mnelo Development (Internal, 1 tester)**. Apple explicitly disables Mnelo Preview external selection while version 0.1.0 build 5 is Waiting for Review. That review and both existing tester memberships remain unchanged. No public store submission or physical build-7 installation is claimed. Upload source reservation/evidence commit: `c2593c1`.

Final build-7 internal-group table explicitly shows **Testing**, iOS, expiry in 90 days. The owner’s tester row still reports installed build 6 at this observation; build-7 installation is not claimed.

# September 12 — build 8 bottom navigation correction

Source correction verified before packaging: the oversized selected tab came from reserving two label lines whenever fontScale exceeded 1.25. Native onTextLayout measurements now determine the shared label height. The actual iOS reproduction lost 71 pixels of unused pill height at 3× scale while retaining the home-indicator clearance. See QA_REPORT for the 388-test and native screenshot evidence.

Build 8 is reserved with the existing identity/signing configuration. Expo Doctor 21/21, dependency alignment, iOS prebuild and CocoaPods install PASS. Signed archive/export/upload results will be appended after those operations actually complete. No dependency or SDK changes.

The owner requested a still-lower menu during compilation. The first archive process was deliberately interrupted (exit 75; its log is retained as build8-archive-interrupted-for-lower-nav.log). iOS now uses an 18-point bottom clearance, preserving full Android system insets. Full QA rerun: 389 tests PASS. Compilation restarted for the same unpublished build number 8 using the updated source. No signing identity, service or data changes.

Final source: `4caf344cf54604d0454258f2cf3155dafcafed69` (preceding measured-label fix `a119148`). Xcode 26.6 Release archive, App Store export and upload **PASS**. Apple acknowledged upload at September 12, 19:33:38 Tbilisi; build `9e4f7ebe-a3ac-44bb-8d3b-7a52d58fcb08` is initially **Processing**. Existing build 5 external review remains Waiting for Review.

Artifacts: `artifacts/Mnelo-0.1.0-8.xcarchive`, `artifacts/Mnelo-0.1.0-8-export/Mnelo.ipa`. Exported IPA: **27,851,887 bytes**, SHA-256 `df616f48726a902e4f3b0a122e3414abeff78bb07e5e2957c30c69c86c8baaff`. Archive and exported Payload both pass six permission descriptions and EN/KA localization checks. Final exported signature validates; bundle `com.mnelo.messenger`, build 8, production APNs, get-task-allow=false and both Mnelo associated domains verified. Compiled Hermes includes the final navigationHomeClearance change, DM Sans/FiraGO and the correct hosted preview endpoints. Native bundle Gitleaks: zero findings; history/source/iOS+Android exports also zero. Actual iOS and Android Hermes exports PASS after the final code change.

The same four upstream dSYM upload warnings remain for React, ReactNativeDependencies, WebRTC and hermesvm; upload succeeded, but vendor-framework crash symbolication remains incomplete. Ordinary upstream compiler warnings remain in the retained archive log. No physical build-8 installation, gesture acceptance or two-phone functional PASS is claimed. No new Android native artifact in this focused UI change.

Final Apple checkpoint, approximately 19:40 Tbilisi: processing **Complete**, compliance cleared using the owner's existing standard-encryption / France No answers, build-specific What to Test saved. Existing **Mnelo Development (Internal, 1 tester)** assigned; its build table explicitly shows **0.1.0 (8) — Testing**, expiry 90 days. Apple continues to disable external assignment until build 5's review completes. No new tester, duplicate identity or cancellation of that review. This is TestFlight availability, not confirmation that the owner has installed build 8.

## 2026-09-12 — Build 9 / smaller, inset selected tabs

Source `9aa397be20c15a1c7182aafddd982ecb7c8f6f32`. Version 0.1.0 (9), com.mnelo.messenger, CS6GJ2BMS9. The Expo prebuild recreated iOS, so CocoaPods was installed to reproduce native dependencies; no generated code patch. Xcode 26.6 signed Release archive and IPA export both PASS. Upload reports **Uploaded Mnelo / EXPORT SUCCEEDED** on September 12 at approximately 20:15 Tbilisi.

Archive: `artifacts/Mnelo-0.1.0-9.xcarchive`. Distribution artifact: `artifacts/Mnelo-0.1.0-9-export/Mnelo.ipa`, 27,851,875 bytes; SHA-256 `0a6785a5089fecb7177f6c1a51922b6a07e7f4f9d978ebb0d7614b0e6d780d8d`. Verified application identifier, version/build, signature, production APNs, disabled debugging, associated domains, all six permission purpose strings with English/Georgian translations. Compiled Hermes contains hosted test endpoints and the new navigationPillWidth token. Native bundle secret scan: zero findings.

Full QA 389 tests PASS; Doctor 21/21 and compatibility PASS. iOS and Android Hermes exports PASS; Gitleaks history/source/bundles zero findings. Preflight has no configuration blockers but explicitly requires live device validation. Four existing vendor dSYM upload warnings persist (React, ReactNativeDependencies, WebRTC, hermesvm).

Distribution limitation: App Store Connect browser redirected to login with authResult=FAILED after upload. Owner reauthentication requested. Apple processing/build identifier, compliance assignment and internal-group availability for build 9 are not yet verified. No review was cancelled or tester removed. Do not present upload as TestFlight availability.

## September 12 — public release audit and USB second-device preparation

Apple live inspection confirms 0.1.0 (9), build fb94f20f-7e9d-43dc-b5bb-dba7236363ce, processing Complete, assigned to Mnelo Development. Build 5 external review remains waiting. No new native archive/upload was needed for unchanged app source. Public Version 1.0 remains Prepare for Submission; product description and URLs saved only as draft. See PUBLIC_RELEASE_READINESS for actual checks and blockers.

The existing build-9 archive uses a development provisioning profile for one registered iPhone (expiry 2027-09-12), unlike the separately exported production-APNs App Store IPA. The second device must be detected/provisioned before USB installation. Never install the App Store IPA as if it were an ad hoc/development artifact.

## September 12 — second physical iPhone USB build, install and boot

Phase: owner-requested direct installation while external Beta App Review is pending.
Status: native Release build, signing, installation and actual registration-screen render PASS; second-phone OTP and two-phone communication acceptance pending.
Implemented: provisioned the newly paired iPhone Air through normal Xcode automatic signing after the owner enabled Developer Mode. Existing bundle `com.mnelo.messenger`, team `CS6GJ2BMS9`, version 0.1.0 (9), Expo 57.0.22 / RN 0.86.3 / Xcode 26.6 retained. No mobile source, generated native code or first-phone app/vault change.
Files: native evidence in ignored artifacts; BUILD_LOG, QA_REPORT, TWO_IPHONE_TESTING, BETA_TEST_PLAN_KA and PUBLIC_RELEASE_READINESS documentation.
Database migrations: none. No server mutation or identity replacement.
Security impact: Xcode's development profile now covers both phones and expires September 12, 2027. Actual code signature verifies, get-task-allow=true, APNs environment development (sandbox); the first phone's TestFlight build uses production APNs. This is development distribution, not a public production build.
Tests: same mobile source passed the preceding 389-test foundation/release audit. The newly compiled native bundle's Gitleaks scan reports 0 findings. All six iOS permission strings and EN/KA translations pass; no always/background location permission. Embedded 7,028,025-byte Hermes bundle contains the existing hosted preview endpoints; SHA-256 `ce6bf0e6ac57da6745ea23d3f36370910ed208cd79eeb85eaf4919538c0db58e`.
Native checks: xcodebuild Release build exit 0 / BUILD SUCCEEDED, zero compiler error lines. Existing dependency compiler warnings remain in the full log, including libwebp conditional-uninitialized and upstream deprecations. No warnings suppressed. devicectl install and launch exit 0 at approximately 22:18 Tbilisi; running Mnelo process and Xcode installed build 9 confirmed. Actual iPhone screenshot shows registration form with native numeric keyboard. No Metro required. Original pre-Developer-Mode attempt exited 70 with destination unavailable before compilation; retained separately.
Artifact: `artifacts/ios-beta-derived/Build/Products/Release-iphoneos/Mnelo.app`. Logs: `second-iphone-build.log`, `second-iphone-before-developer-mode.log`, install/launch/process JSON and logs, build-verification JSON and bundle-secret-scan report.
Known limitations: no second-phone OTP, push receipt, calls/audio/video or two-way messages PASS yet. Combined identity/relay, TURN and Caddy are active; the intentionally disabled standalone relay must not be started on the same port. Read-only server aggregate checks are routing prerequisites, not device acceptance. External Apple review and public-release blockers remain unchanged.
Commit: this checkpoint accompanies `chore: install build 9 on second physical iPhone`.

Second-phone enrollment checkpoint: the owner reports entering the app and finding the first account by phone. Read-only checks confirm both admitted numbers registered and new sandbox alert + VoIP routes for the second phone. SMS code/credentials were never collected. Zero peer wake grants at this checkpoint; real content/push/calls are not yet PASS. The owner found that new-chat search does not handle national numbers and phone discovery exposes a full-key/manual-name confirmation flow. This UX issue is now the active follow-up; no protocol test has been removed.

# September 12 — build 10 contact/chat fixes checkpoint

The owner reported failed QR scanning on two physical build 9 iPhones. The installed Expo Camera 57.0.5 requires `ExpoCameraZXingProvider` even for the selected QR scanner path. The companion pod was absent from the generated lockfile. The CocoaPods autolinker attempted `Array.filter_map` under Ruby 2.6 and omitted companion metadata. Explicit, reproducible `expo-build-properties` extra pods now include ExpoCameraBarcodeScanning plus modular ZXing subspecs; no generated Swift patch or Expo version change was used.

`artifacts/iphone-build10.log` ended **BUILD SUCCEEDED** with Xcode 26.6. The strengthened native guard checks the defined Objective-C provider class using `nm -U`: build 9 fails, build 10 passes. A plain binary string search is insufficient because Camera embeds the provider lookup name without linking the class. Six localized permission descriptions and no always-location usage remain checked. Later JS/UI changes require another incremental build before installation. No build 10 two-phone QR/audio/video PASS is claimed.

Owner's voice-call reproduction ended with “Call ended”; the existing device console did not expose a cause. Safe native failure and negotiation-stage codes were added. Ringing does not establish successful media. The subsequently authorized asynchronous Signal delivery migration is documented in DELIVERY_ARCHITECTURE_REVIEW; it has not been deployed at this checkpoint.

# September 13 — Signal native integration checkpoint

Official libsignal 0.102.2 Swift module and final Release iPhone app build PASS on Xcode 26.6. First link failed with unresolved vendor FFI symbols under static CocoaPods frameworks; the local podspec now propagates the pinned Rust archive to the final linker. No SDK downgrade or vendor-source patch. Android Kotlin module compile PASS on isolated JDK 21.0.12.1+1 / Gradle 9.3.1. Full new Android APK, native cross-platform encryption runtime, latest UI installation and two-phone calls remain unverified. Detailed evidence, deployment state and license gate: [Signal implementation](SIGNAL_IMPLEMENTATION.md).

# September 13 — executed Swift/Java Signal interoperability

Xcode 26.6 Release iOS Simulator build completed successfully with the separate synthetic probe entry. The installed iPhone 17 Pro / iOS 26.5 simulator executed 10 protocol checks; the independent Rust-backed Node implementation decrypted the resulting Swift reply. Evidence: native-signal-simulator-build.log, native-signal-interop-result.log and native-signal-probe/simulator-result.png.

The first complete Android Release APK failed at `:app:checkReleaseAarMetadata`: `org.signal:libsignal-android:0.102.2` requires core library desugaring. The installed vendor AAR metadata explicitly declares `coreLibraryDesugaringEnabled=true`. The Expo `with-signal.cjs` plugin now enables this and pins Google's desugar_jdk_libs 2.1.5; source/target compatibility stays unchanged. This is a reproducible generated-project configuration, not a generated Gradle file patch. [Official Android guidance](https://developer.android.com/studio/write/java8-support), [pinned vendor build file](https://github.com/signalapp/libsignal/blob/v0.102.2/java/android/build.gradle), [Google desugaring changelog](https://github.com/google/desugar_jdk_libs/blob/master/CHANGELOG.md).

Android prebuild PASS. Complete ARM64 Release probe APK build then succeeded in 2m37s (1,095 tasks), with existing Gradle 10 deprecation warnings. A subsequent probe-only JS rebuild also passed. The API 36 emulator executed the same 10 native checks, and independent Node/libsignal decrypted its Java-native reply. Evidence: native-signal-android-build.log (retained failure), native-signal-android-build-desugar.log, native-signal-android-probe-report-build.log and native-signal-android-interop-result.log.

Probe entries never import the real account/runtime or a user vault. The APK at android/app/build/outputs/apk/release/app-release.apk and simulator app at artifacts/ios-beta-derived/Build/Products/Release-iphonesimulator/Mnelo.app currently contain this isolated test UI, not the product, and must not be distributed. Synthetic fixture/state files stay ignored and mode 0600; reports contain check names and synthetic ciphertext only. Both physical iPhones remain unchanged. Full product-native and physical call acceptance are pending.

# September 13 — final development iPhone product build 10

After native interoperability and hosted delivery activation, the real `index.js` product entry was built with explicit preview endpoints and `EXPO_PUBLIC_DELIVERY_V2=1`. iOS prebuild and CocoaPods installation PASS (135 dependencies / 137 pods). CocoaPods/Ruby 2.6 emits upstream `filter_map` companion-config warnings; modules compile from source, and the explicit QR provider is verified in the final linked binary. No warning suppression or generated vendor edit.

Xcode 26.6 final Release iPhone build **BUILD SUCCEEDED**. Codesign deep/strict verification PASS; bundle com.mnelo.messenger, team CS6GJ2BMS9, version 0.1.0 (10), development signing/APNs, provisioning includes both phones. Six purpose descriptions, EN/KA values and defined ExpoCameraZXingProvider class PASS. This is a USB development `.app`, not an App Store IPA or a new TestFlight upload.

Artifact: `artifacts/ios-beta-derived/Build/Products/Release-iphoneos/Mnelo.app`. Final Hermes bundle: 7277578 bytes, SHA-256 `8244c8d01777182dec2b0017946cd23198ad52baa53e46de071d8dc4547ab12a`. Both hosted endpoints, new delivery retention copy and the recovery guard are verified in the compiled bundle; the synthetic native-probe entry is absent. Native-app secret scan: zero findings. Logs: delivery-product-iphone-final-build.log, delivery-product-final-permissions.log, delivery-product-final-verification.json. Both mobile exports also pass; Android's latest complete APK remains the isolated native probe, not a new full product artifact.

Neither physical iPhone was updated or erased. Licensing, signed-key lifecycle/recovery and two-device physical acceptance remain documented release gates. Do not install an old protocol build over a migrated vault or restore old ratchet state to roll back.

## September 13 — TestFlight build 11 preparation

The owner explicitly requested a new TestFlight upload for later physical testing. Apple UI confirms build 9 is the latest upload; build 5 remains Waiting for Review for the existing external group. Build 10 is a local development product/probe checkpoint and was not uploaded. Build 11 retains version 0.1.0, bundle com.mnelo.messenger and team CS6GJ2BMS9.

The testflight profile now explicitly selects EXPO_PUBLIC_DELIVERY_V2=1 with the existing hosted preview endpoints. Other profiles remain unchanged. Both participants need the new version for the Signal delivery path; never downgrade a migrated vault or uninstall to troubleshoot. The guarded recovery limitation remains visible in the app. This is a scoped functional beta, not public production approval.

The in-app source/license links pin the public source tag ios-0.1.0-11, which must resolve to the archived source before upload. Existing native/third-party licenses are retained. The owner's open-source decision authorizes source publication; it is not a security certification or new legal declaration. No Apple account, signing identity or tester membership is replaced. Archive/export/upload results are recorded only after execution.

Foundation checks PASS: 304 Jest tests in 61 suites, 3 server checks and 131 device/protocol checks (438 total), TypeScript, lint, formatting, environment/security/localization checks. Generated iOS prebuild and six permission descriptions with EN/KA translations PASS.

The first CocoaPods invocation from the repository root with `--project-directory=ios` failed to resolve LibSignalClient 0.102.2. Installed Expo autolinking resolves `apple.extraPods` against the command working directory: the root invocation returns no extra dependencies, while the same resolver inside `ios` returns all four configured pods. Running CocoaPods **inside `ios`** with the existing scoped Ruby environment succeeded: 135 dependencies / 137 pods. No package version, generated source or dependency policy changed. The separate known Ruby 2.6 `filter_map` warnings retain source compilation; they did not cause this missing-pod failure. Logs: build11-pods.log (failure retained), build11-pods-ios-cwd.log (success).

Source `58665f4c5aae5a32ec950a7fe971212a82a13e7d` was archived successfully with Xcode 26.6, the real `index.js` entry and explicit preview/delivery-v2 environment. Apple-managed App Store export PASS. The exported IPA passes deep/strict signing, exact bundle/build/team, production APNs, get-task-allow=false and both associated domains. Archive and exported payload have identical Hermes SHA-256 `fae624e8880dffabbc344cd3b9fb38a7e506c7b852206c7557598dc8bcf84f18`. IPA: `artifacts/Mnelo-0.1.0-11-export/Mnelo.ipa`, 57,634,168 bytes, SHA-256 `9213662f6a49d4f75330fceea08d9d26b6e5a3e6425e8a3b292cb762f86181ed`.

The initial archive QR check produced a false negative: Apple archive stripping removes its local class symbol, while matching app/dSYM UUID DA8AFE49-3B85-3D3A-975D-1A0C819A70F9 proves the provider was linked. The checker now falls back to the actual Objective-C `__objc_classlist` definition in the final executable, not a lookup string or class reference. Both archive and exported payload PASS all six purpose strings, EN/KA and provider checks. The old build 9 correctly FAILS the same provider check, preserving regression detection. No native runtime code or archived source was changed by this verification-only correction. Physical QR scanning remains pending.

Exact corresponding source is published at [ios-0.1.0-11](https://github.com/Mneloapp/mnelo-app/tree/ios-0.1.0-11), public commit `01f637b8e33a2ed383543828e6dd35319044225f`, tree `277587afc188138ecddadec9c1bae9feaa384deb` matching the archived local source. [Public CI](https://github.com/Mneloapp/mnelo-app/actions/runs/34747351583) PASS. Expo Doctor 21/21 PASS. Remaining compiler/vendor deprecation warnings are retained in build11-archive.log; no diagnostic was disabled.

Xcode upload **PASS** (`Uploaded Mnelo`, `EXPORT SUCCEEDED`, exit 0). Apple UI lists version 0.1.0 (11), upload time September 13 12:26, build ID `6e484dc1-7209-4889-9aeb-1fcb1e1146e9`, initially Processing. Four existing vendor dSYM warnings remain (React, ReactNativeDependencies, WebRTC, hermesvm), affecting crash symbolication, not upload completion. Final native-app secret scan and 49.93 MB disassembled Hermes scan both report zero findings. Logs: build11-upload.log, build11-native-secrets.log, build11-hermes-secrets.log. Tester access/compliance/review results will be recorded after Apple processing; uploading alone does not send an external invitation.

Apple processing subsequently **Complete**. Build-specific compliance cleared with the existing owner-authorized standard encryption in addition to the OS / France No answers, accounting for the expanded libsignal inventory. Added the existing Mnelo Development internal group (one tester), saved the 1,618-character physical-test instructions, and removed obsolete build 5 from Beta App Review so it would not block the current corrected candidate. Added Mnelo Preview (existing external group, one tester) to build 11 and submitted it with **Automatically notify testers enabled**. UI verifies build 11 **Waiting for Review**, both groups assigned; build 5 is Ready to Submit. External approval/email delivery is still pending. No public App Store submission, new account role, new App ID or tester identity was created.

Phase: TestFlight preparation and upload. Status: COMPLETE for engineering/upload/submission; Apple external approval and physical acceptance pending. Implemented: build reservation, explicit delivery-v2 profile, exact source link/tag, archive/export/upload, stripped-binary QR verification and tester instructions. Database migrations: none. Security impact: existing preview endpoints/admission preserved; no secrets in source/binary; no new physical or independent-audit claim. Native checks: actual archive and IPA verified. Commit: source `58665f4`, native verification/upload `dd69f36`; final Apple disposition is in the subsequent documentation commit. Full checks and both public CI runs pass.

## September 13 — physical-feedback UI compilation

Source verification: 447 tests (312 Jest / 3 server / 132 device-protocol), TypeScript, lint, formatting and repository source checks PASS; Expo Doctor 21/21 and dependency compatibility PASS. iOS and Android production-mode Hermes exports completed with explicit preview URLs and delivery V2; output `artifacts/ui-refinement-export`, log `artifacts/ui-refinement-export.log`.

Xcode **26.6 (17F113)**: actual generic-iOS **Release build PASS**, exit 0 / `BUILD SUCCEEDED`, with the existing workspace/pod state, four compiler jobs and `CODE_SIGNING_ALLOWED=NO`. This is an unsigned compilation diagnostic, not an installable/TestFlight export. No SDK, Podfile, provisioning, App ID or build-number change. Output: `artifacts/ios-beta-derived/Build/Products/Release-iphoneos/Mnelo.app`; log `artifacts/ui-refinement-ios-build.log`. App Hermes SHA-256: `fc0d981d82983220fac6ad7f039f361fd96a84013f98ec4edefc3c892ee758a5`. The six iOS purpose strings, EN/KA translations and linked QR provider check passed against the completed product. Existing dependency deprecation/script warnings remain visible; no compiler diagnostic was suppressed.

Visual component review: actual React Native Web components at 390×844, 320×568 and 430×932, EN/KA. External reaction badges, compact leaves, directions/red missed calls, responsive icon action sheets, backdrop dismissal and full-screen photo close/share controls checked. No horizontal overflow in the 320-point call view. Preview startup initially failed because its temporary harness did not proxy font assets; corrected the local harness. Removed an unsupported SVG `accessible=false` web attribute warning; the final browser console contained only React development informational output. Preview screenshots and harness live under ignored `artifacts/chat-ui-review/`, using synthetic text and the Mnelo brand asset, not private user images. These are browser component previews, not physical-device screenshots. iOS/Android system typography, Photos sharing and native insets still require phone QA.

There was no new archive/upload, Apple review change, device install, server deployment or data reset in this step. Build 11 and its immutable corresponding-source tag remain the previously distributed artifact. The next distribution build must include these changes and both test phones must be updated before repeating two-device acceptance.
