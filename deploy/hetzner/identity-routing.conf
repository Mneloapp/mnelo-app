# Install only with the verified cohort-aware runtime. Stop/disable the old
# standalone mnelo-relay first; keep its unit for code-only rollback.
[Service]
Environment=MNELO_COMBINED_RELAY=1
SocketBindAllow=tcp:8084
