# Development media fixtures

`development-tone.m4a` is a generated one-second 440 Hz AAC test tone. It contains no recorded voice or personal information. It exercises actual server container/codec/duration validation in local integration tests and is never bundled as product content.
