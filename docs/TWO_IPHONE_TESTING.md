# Two-iPhone TestFlight preparation

> Updated owner acceptance: background/closed-app message notifications and system incoming calls are mandatory before the two-phone beta is considered ready. Build 2 is preserved as evidence, not a completed candidate. Native remote push and CallKit implementation/physical acceptance are now required.

> Current September 12 update: the owner-authorized functional TestFlight beta has a scoped engineering review in [BETA_SECURITY_REVIEW](BETA_SECURITY_REVIEW.md). Exact development endpoints and the existing two-number admission are required; production and independent-audit claims remain blocked. Earlier blanket preview-blocking and absent-TURN statements below are historical. Hosted TURN now passes real browser UDP/TCP data/media tests; physical-device and Apple-distribution results are recorded separately.

## Current delivery checkpoint — September 12, build 9 on both phones

The second phone is now physically available. Xcode automatic signing provisioned the iPhone Air after the owner enabled Developer Mode. A fresh standalone Release build of 0.1.0 (9), `com.mnelo.messenger`, completed with Xcode 26.6; `devicectl` installation and launch succeeded at approximately 22:18 Tbilisi. Xcode confirms build 9 installed, and an actual device screenshot shows the registration screen and working phone-number keyboard. The first iPhone's existing build 9 was also confirmed in Xcode; it was not reinstalled or reset.

The new development provisioning profile covers both registered phones. The second phone uses APNs development/sandbox and embedded hosted-preview JavaScript; the existing first-phone TestFlight binary uses production APNs. No Metro connection is needed for the USB Release build. Signing verification, six iOS permission strings with EN/KA translations, hosted endpoints and compiled-JS secret scan all pass. The prior destination-timeout failure occurred before compilation while Developer Mode/pairing was incomplete; the subsequent build succeeded without source or generated-code patches.

**Installation, registration-screen rendering and owner-confirmed second-phone enrollment are verified; two-way functional acceptance is pending.** A read-only, redacted server check before enrollment confirms the first admitted number is registered and the second is not. No identity binding was deleted or replaced, no credentials were displayed, and no SMS was sent by the check. After the owner completed registration, a second read-only check confirmed both admitted numbers registered and the new second-phone sandbox alert/VoIP routes present. This does not prove push receipt or calls. The owner then reported friction in number discovery; simplifying that flow is the next code task. Follow [the Georgian test guide](BETA_TEST_PLAN_KA.md). External build 5 review and the existing tester memberships were left intact. Public submission remains blocked as documented in [PUBLIC_RELEASE_READINESS](PUBLIC_RELEASE_READINESS.md).

## Historical delivery checkpoint — September 12, TestFlight build 5

**Build 5 is submitted for external Beta App Review.** Apple upload processing is Complete; the owner-authorized standard-encryption / France No answers cleared Missing Compliance. Mnelo Preview shows **Waiting for Review**, one tester and one build. **Automatically notify testers** was enabled on submission at approximately 14:16 Tbilisi. No external invitation delivery or second-phone installation is claimed before approval.

- **Current artifact:** 0.1.0 (5), com.mnelo.messenger, unchanged team. Signed Release archive/export/upload PASS. Build ID `fc21f388-12f0-4c99-9f92-5467fb617612`; upload 14:07:07 Tbilisi. Production APNs entitlement, six permission strings/EN+KA, final signature, hosted endpoints and 16 privacy manifests verified. Four existing vendor dSYM warnings remain. Build 4 repaired the earlier ITMS-90683; builds 2/3 remain failed historical evidence.
- **Quality:** 340 automated tests PASS (246 Jest, 91 device/provider, 3 historical utility tests); TypeScript/lint/format, Doctor 21/21, compatibility, both mobile exports and secret scans PASS. Latest dependency audit remains 16 moderate/zero high-critical, not rerun for this no-dependency-change work. No new Android native/device QA is claimed.
- **First iPhone:** owner confirms TestFlight build 4 opened and Notifications/Allow was enabled. Build 5 is also assigned to the existing internal Mnelo Development group, which still has exactly one account-holder tester; automatic distribution of future builds remains off. Preserve the installed identity/history with an in-place update, never uninstall/reset/re-enroll. Build 5 installation and history retention are not yet physically verified.
- **Second iPhone:** the exact owner-authorized external tester remains in Mnelo Preview. The app becomes downloadable after Apple's review; automatic notification is configured, not proof an email already arrived. No public link or team role was created.
- **Reviewer access:** two owner-authorized fictional reviewer accounts with independent expiring 128-bit access keys are deployed, isolated from real users. Actual keys/instructions are saved only in Apple's private review information and owner-private files. Hosted HTTPS enrollment/lookup/TURN and WSS signed message/call offers pass in both directions, followed by self-unlink of the synthetic probe identities. No SMS/APNs push or physical media was generated. [Reviewer boundaries](APPLE_REVIEW_ACCESS.md).
- **Preserved state:** after rollout/cleanup the original one real identity, two Production push routes and zero wake grants remain. Identity/SMS-budget/push records and index/provider/APNs keys were verified preserved. The same VM now runs current identity/relay authorization together; standalone relay disabled, Caddy/TURN unchanged. No content/history queue or new paid infrastructure.
- **Providers:** Infobip and Sandbox/Production APNs remain configured. Android Firebase client/sender setup is incomplete. Push registration is not actual receipt; two-phone alerts/system ringing/media/reconnect remain acceptance tests.

Use the [Georgian test guide](BETA_TEST_PLAN_KA.md). **First-phone internal testing is available; second-phone external distribution is waiting for Apple.** The full two-phone background/call acceptance is not yet PASS. Generic push is not content delivery: the sender's local copy must become reachable to transfer pending content. Public release and independent-security-review gates remain open.

## Historical checkpoints

The remaining dated notes preserve prior findings and are superseded by the current delivery checkpoint above. In particular, statements that there is no TURN, archive, IPA, upload, funded provider or registered first phone are historical, not current blockers.

## Hosting update — 2026-09-12

Hetzner owner login/payment prerequisites are complete. **mnelo-dev-01** is provisioned and the development HTTPS identity and WSS relay services pass synthetic authorization/routing checks; no real SMS was sent. Actual price is **$7.09/month excluding VAT**. See [current hosting evidence](DEVELOPMENT_HOSTING.md) for IDs, endpoints, firewall, isolation and limits. The September 11 account-access-pending statements below are historical and superseded.

Infobip payment is complete: live balance **$20**, pay-as-you-go enabled, 14 free SMS remaining after the first live registration, automatic recharge unconfigured. The owner selected two test numbers; exactly those two are now admitted on Mnelo Development. The owner confirms SMS receipt and successful code entry on the first iPhone. Hosted state independently shows one registered identity and one SMS reservation per hour/day window; the other number remains untested.

A private, ignored `.env.development.local` now selects the hosted HTTPS identity and WSS relay; the original `.env.local` is preserved. Metro was restarted on port 8083 and advertises the existing `com.mnelo.messenger` development client. This is an actual development session, **not a TestFlight release relabeled to evade review**. The installed development binary remains version 0.1.0 (1); no new native binary, IPA/upload or invitation was created.

The first launch attempt was rejected while the iPhone was locked. After the owner unlocked it, launch succeeded and an actual Xcode screenshot shows Chats after the owner completed registration. This is first-phone auth/initial-render evidence; it is not a new native build or two-phone messaging/call PASS. The second iPhone is remote and not available for USB installation; the owner selected TestFlight delivery and subsequent updates. TURN is still absent, two-phone OTP/media are untested, and protocol review/Apple distribution remain open. **Still NOT READY for complete two-phone/TestFlight acceptance.**

## Verified checkpoint — 2026-09-11

Owner confirms both test devices are iPhones. Target is a private two-person test through TestFlight. No public App Store release, export-compliance declaration, contract acceptance, invite or purchase was performed.

| Prerequisite               | Actual state                                                                                                                                                                                                                                                                            |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Source revision at audit   | `620367320b21acf17d52530c12ff4fe78e3a270c`                                                                                                                                                                                                                                              |
| Native identity            | `com.mnelo.messenger`, owner-approved and already provisioned for development                                                                                                                                                                                                           |
| Xcode                      | 26.6 (17F113)                                                                                                                                                                                                                                                                           |
| Connected hardware         | One paired iPhone 17 Pro Max available; second iPhone not connected in this inventory                                                                                                                                                                                                   |
| Signing                    | One local Apple Development identity; zero local Apple Distribution identities. Xcode-managed distribution signing still needs verification; absence locally does not prove the account lacks distribution rights.                                                                      |
| App Store Connect          | Owner login completed. Created **Mnelo**, Apple app ID **6811153275**, using the existing `com.mnelo.messenger` Bundle ID; English (U.S.), SKU `mnelo-messenger-ios`. Draft status: Prepare for Submission. TestFlight opens and states that a build must be uploaded. No build exists. |
| Expo/EAS                   | Cached EAS CLI 23.2.0 `whoami`: Not logged in. An EAS login is not required to use local Xcode archive/upload.                                                                                                                                                                          |
| Existing physical artifact | `artifacts/iphone-20260911-build/Build/Products/Debug-iphoneos/Mnelo.app`: earlier signed Debug development client, not a TestFlight archive/IPA.                                                                                                                                       |
| SMS endpoint               | Saved local configuration resolves to HTTP loopback port 8086. An iPhone's loopback is that iPhone, not this Mac. Live read-only Infobip application/template validation passed again in this checkpoint, with no SMS sent; two-phone carrier OTP is untested.                          |
| Signaling endpoint         | Saved local configuration resolves to WebSocket loopback port 8084. No reachable WSS deployment is configured.                                                                                                                                                                          |
| Internet connectivity      | PeerMesh currently receives an empty ICE-server list; no STUN/TURN setup. Wi-Fi/cellular connectivity must not be assumed.                                                                                                                                                              |
| Store configuration        | EAS production uses store distribution; development/preview internal profiles are not TestFlight distribution profiles. Preview/production still fail the intentional protocol-review gate.                                                                                             |
| Release review             | `privacyRelease.reviewed` remains false. Do not relabel a release as development, disable the gate or claim an independent audit merely to upload a build.                                                                                                                              |
| Result                     | **NOT READY for TestFlight upload or complete two-phone acceptance. No new IPA or upload/build ID.**                                                                                                                                                                                    |

## Apple record and next owner action

The owner completed Apple login while the audit was running. The selected team's app list did not contain Mnelo; the new-app form offered the already registered `com.mnelo.messenger` identifier. Created the Mnelo app record without creating another App ID. Verified it after refreshing the Apps list, then opened its empty TestFlight Builds page. `eas.json` now pins `submit.production.ios.ascAppId` to **6811153275**. No other app, user role, certificate or provisioning profile was changed. Apple's form allowed only Full Access; no new team member was added or invited.

[Mnelo in App Store Connect](https://appstoreconnect.apple.com/apps/6811153275/distribution) is a draft record, not a published/downloadable application. Apple's initial draft version is 1.0; repository version remains 0.1.0. Reconcile intended version/build numbers during the actual archive, not by inventing an uploaded build. TestFlight access is verified; no upload, processing, beta review, tester invite or public release has happened.

The next owner action is sign in or register at the prepared [Hetzner Console](https://console.hetzner.com/) tab so an owner-controlled development server can be selected. No server order, payment, account registration, DNS change or provider-secret transfer was performed. **Later on 2026-09-11 the owner explicitly authorized the proposed small rented server for up to approximately 50 testers.** Do not request that routine purchase permission again; owner login, payment details and contractual acceptance still require the owner. First validate on the two iPhones before expanding the cohort. [Current hosting plan and bounded local capacity check](DEVELOPMENT_HOSTING.md). Do not put passwords, authentication codes or server secrets in chat.

Proposed hosting shape: one small Linux VM for the existing HTTPS identity service, transient WSS signaling and a reviewed TURN forwarding path. A CX23 EU server has a published base of **€5.49/month excluding IPv4 and VAT** as of this check; verify availability, the actual account's currency/tax and final total before any order. No backups/snapshots or other paid additions are preselected. This is a development proposal, not a proven production hosting/compliance result. See [Hetzner's current price table](https://docs.hetzner.com/general/infrastructure-and-availability/price-adjustment/) and [server creation documentation](https://docs.hetzner.com/cloud/servers/getting-started/creating-a-server/).

Live DNS lookup shows `mnelo.com` uses Vercel nameservers; they and the existing website are unchanged. Vercel now supports WebSockets in public beta, so older claims of no WebSocket support are obsolete. Its functions still need shared state across instances. Moving this repository's single-process relay, private SQLite phone registry and UDP TURN path there would require a separate redesign; a small VM preserves the current server structure. References: [Vercel WebSockets](https://vercel.com/docs/functions/websockets). No website was overwritten or migrated.

The service deployment and independent protocol review are still open engineering prerequisites. Use stable reviewed HTTPS/WSS endpoints and retain the private phone-index key/registry without message, attachment or call storage. Signaling remains transient. Do not publicly expose the fictional-code fixture, embed provider keys, assume a temporary tunnel is a durable TestFlight backend, relabel a release as development, or rewrite release safeguards. Remote access alone does not certify privacy or physical-device behavior.

Before live OTP, inspect the actual Infobip verified-recipient allowance for both testers. The previous account trial was verified only for the owner's recipient. Do not assume a second number is eligible, upgrade billing automatically or count provider acceptance as receipt. Both testers enter their own codes on their own phones. Enrollment alone does not recover keys or transfer a previous identity.

## Distribution sequence after prerequisites

1. Preserve existing keys, histories, bundle identity and the selected Apple team. Review the device-owned protocol and resolve its explicit release gate with evidence.
2. Configure and verify the actual development HTTPS/WSS endpoints without public secrets or historical Supabase URLs. Verify signed identity requests and rejected unauthorized requests; never trigger billable OTP automatically from a readiness probe.
3. Validate the two SMS recipients and real enrollment in a development build. Compare and pin each other's full Mnelo codes through a trusted channel before messaging; both peers currently add each other.
4. Use the authenticated owner's Mnelo App Store Connect app record (6811153275) and Xcode signing. Build a standalone, signed iOS archive with embedded JavaScript and no Metro dependency. Validate the final embedded environment, entitlements, permissions, secrets and signature.
5. The owner completes Apple's encryption/export-compliance and any legal declarations based on the actual SQLCipher/backup/signing/WebRTC implementation. Do not set `ITSAppUsesNonExemptEncryption = false` by assumption.
6. Upload for TestFlight. For App Store Connect team users, use **TestFlight Internal Only** when appropriate. Do not grant a second tester broad App Store Connect roles merely to avoid external beta review; an external tester may require the external TestFlight path. Do not publish to the public App Store.
7. Wait for actual Apple processing and tester eligibility, then install the processed build on both iPhones using TestFlight. Record exact version/build ID and tested device/OS, not screenshots from a simulator.

Apple references: [internal testers](https://developer.apple.com/help/app-store-connect/test-a-beta-version/add-internal-testers), [app record](https://developer.apple.com/help/app-store-connect/create-an-app-record/add-a-new-app/), [uploading builds](https://developer.apple.com/help/app-store-connect/manage-builds/upload-builds). Expo reference: [TestFlight distribution](https://docs.expo.dev/submit/testflight/). Internal EAS ad hoc distribution is distinct from Apple TestFlight store distribution.

## Two-phone acceptance record

All rows below still require **two-phone acceptance**. The September 12 first-phone SMS/OTP and Chats render are verified above; do not count that as both phones passing.

| Scenario               | Acceptance evidence                                                                                                                                                   |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Install / cold launch  | Same standalone build on A/B; correct icon/name; opens with Metro stopped                                                                                             |
| Registration           | Real code received and accepted on each phone; invalid code rejected; no SMS on ordinary reopen                                                                       |
| Peer identity          | A/B compare and pin full Mnelo codes; lookup cannot replace an existing key silently                                                                                  |
| Direct/group messaging | Send both ways; unread/read; replies/reactions; group membership rules; independent deletion                                                                          |
| Media                  | Photo, file, voice recording/playback, location and contact sharing; permission denial/retry                                                                          |
| Calls                  | Voice/video both ways; ringing, accept/reject/end; camera/mic denial and interruptions                                                                                |
| Connectivity           | Same Wi-Fi, different Wi-Fi/cellular, airplane mode/reconnect; pending state/duplicates                                                                               |
| Background             | Foreground → background → locked → terminated; document unsupported behavior. Current local notifications do not promise incoming delivery to a suspended/closed app. |
| Privacy / recovery     | Number hiding; blocks; own backup restore; other participant's independent history preserved                                                                          |

## Repeatable local preflight

```sh
npm run testflight:preflight
```

This command reads the production-mode local dotenv resolution and signing-identity availability without exposing values, exporting credentials, sending SMS or mutating Apple accounts. It reports local configuration blockers and returns exit 1 when they exist; passing local prerequisites only advances to live validation, never certifies store/device readiness. Hosted EAS environment settings must be checked separately when an actual project is linked.

September 11 historical result: exit **1**, `LOCAL_PREREQUISITES_BLOCKED`: `TESTFLIGHT_ENV_NOT_SELECTED`, `MESSENGER_SECURITY_REVIEW_REQUIRED`, `REMOTE_HTTPS_REQUIRED`, `REMOTE_WSS_REQUIRED`. The current default `testflight` profile selects preview and the deployed services; its local preflight still exits 1, now specifically for `MESSENGER_SECURITY_REVIEW_REQUIRED`. Selecting a release environment alone cannot satisfy the remaining gates. Sanitized machine-readable snapshot: ignored `artifacts/testflight-preflight-20260911.json`.

## Remote tester and update plan — 2026-09-12

The owner cannot connect the second iPhone to this Mac and selected remote installation with later app updates. Use an external TestFlight beta for that person, preserving the existing registered app identity. Do not give a tester App Store Connect team privileges just to avoid beta review. Apple's first external beta requires App Review for TestFlight; upload alone is not an available download. No invitation has been sent yet. [Apple TestFlight](https://developer.apple.com/testflight/), [external testing](https://developer.apple.com/help/app-store-connect/test-a-beta-version/invite-external-testers/).

Keep com.mnelo.messenger, its signing team/keychain access and device-vault format stable across versions. Use migrations that preserve local identity, enrollment and conversations; test an in-place upgrade with actual retained data before claiming it passes. Do not uninstall the app to update it. A change of the phone-service origin also requires a deliberate enrollment migration: current enrollment is bound to its service URL. The server registry alone cannot restore a lost local history/private key. Increment build numbers, retain protocol compatibility with the previous beta, and verify new builds start without Metro. User-controlled backups and their recovery remain separate from ordinary app updates.

The prepared `testflight` build profile uses store distribution, preview environment and bundled Release JS, with automatic build-number increments and submit target 6811153275. Current local profile validation passes the installed EAS schema and keeps preview/production review gates intact. An explicitly selected development-client profile is rejected for store upload. See RELEASE.md for the remaining archive/signing/review/upgrade checks. No beta artifact is uploaded yet.
