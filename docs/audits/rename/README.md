# Historical branding-rename evidence

These snapshots predate the owner-confirmed native migration. Connecto is the former working name; retained identifiers and pending-verification statements here are historical, not current configuration. Current product identity is Mnelo, bundle/package `com.mnelo.messenger`. See [current identity](../../PRODUCT_IDENTITY.md).
